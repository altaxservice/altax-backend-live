# Communications + Reports — Legacy vs New App

## Summary
Communications: ~30 legacy items reviewed — roughly 9 Match, 12 Partial, 9 Missing (plus 1 New addition). Reports: ~29 legacy items reviewed — roughly 12 Match, 6 Partial, 11 Missing (plus 1 New "Firm Overview" tab not present in the legacy page).

## Communications

| Legacy Item | Description | New App Status | New App Location | Notes |
|---|---|---|---|---|
| Page eyebrow/title (role-based) | Legacy shows 4 distinct headers: "Firm Communication Center" (admin), "Staff Message Center" (staff), "Client Message Center" (client), "Employee Message Center" (employee), each with its own descriptive copy line. | Partial | `CommunicationsPage.tsx` | New app has only two states: a manager view (admin/staff, undifferentiated) and a generic "Communications" view for everyone else (client/employee treated identically, no distinct copy). |
| Intro/help copy | Explains that Email can send via MailApp after authorization, SMS/WhatsApp open a compose window to confirm manually. | Missing | — | No equivalent explanatory text in new app. |
| "Authorize Email" button | Admin-only button that checks/prompts Google MailApp (Apps Script) email authorization status. | Missing | — | No email-authorization concept in new app (uses a different email backend); no button present. |
| "Reminder Center" button | Admin-only button opening the Reminder Center modal (see rows below). | Missing | — | No Reminder Center anywhere in new app. |
| Reminder Center: Daily Operations Alert panel | Sub-panel with "Disable Daily Alert", "Install Daily Alert", "Send Daily Alert Now" buttons; sends firm + per-staff workload summary emails. | Missing | — | No daily operations alert feature found in new app. |
| Reminder Center: "Due Within Days" input | Number input (default 7) controlling reminder lookahead window. | Missing | — | Not present. |
| Reminder Center: "Channel" dropdown | Options: Email, Portal Note. | Missing | — | Not present. |
| Reminder Center: "Send Email Now?" dropdown | Options: No, Yes. | Missing | — | Not present. |
| Reminder Center: "Audience" dropdown | Options: Staff Only, Clients Only, Clients and Staff, Employees Only. | Missing | — | Not present. |
| Reminder Center: "Daily Schedule Hour" dropdown | Options: 7–12 (hour of day for scheduled alert). | Missing | — | Not present. |
| Reminder Center: "Run Work Reminders Now" button | Creates one-time reminder messages for overdue/due-soon tasks, document requests, and tax payments. | Missing | — | Not present. |
| Reminder Center: "Save Work Reminder Schedule" button | Installs a daily automated trigger with the chosen settings. | Missing | — | Not present. |
| Reminder Center: "Disable Work Reminder Schedule" button | Removes the daily automated trigger. | Missing | — | Not present. |
| Firm Staff Messages panel | Section header "Firm Staff Messages" with message count, shown only to admin/staff/manager/owner roles; excludes clients. | Match | `CommunicationsPage.tsx` → `StaffMessages` component | Panel title, note text, and count are present. New app's role gate is admin/staff only (new app has no manager/owner role type, so this is effectively equivalent). |
| Staff composer: recipient dropdown | "Staff / Manager / Admin" select, populated from active staff/admin/manager/owner users with email. | Match | `StaffMessages` component | Uses `/communications/staff-directory`; label text matches. |
| Staff composer: "Send Email Now?" dropdown | Options: No, Yes. | Missing | — | Not present in new staff composer form. |
| Staff composer: "SMS / WhatsApp Phone" input | Free-text phone field, pre-filled from selected staff record. | Missing | — | Not present. |
| Staff composer: "SMS Gateway Email" input | Optional carrier SMS gateway email field. | Missing | — | Not present. |
| Staff composer: "Subject" input | Default "Firm staff message". | Match | `StaffMessages` component | Same default text. |
| Staff composer: "Message" textarea | Placeholder "Write the staff message or task update here." | Match | `StaffMessages` component | Same placeholder text. |
| Staff composer: Channels | Checkbox grid, multi-select: Email, SMS, WhatsApp, Phone (Email checked by default). | Partial | `StaffMessages` component | New app uses a single-select "Channel" dropdown (Email, SMS, WhatsApp, Phone, Portal Note) instead of multi-select checkboxes — only one channel can be sent per submission. |
| Staff composer: "Send / Save Staff Message" button | Submits and saves/sends across selected channels. | Match | `StaffMessages` component | Button label matches exactly. |
| Staff message history table | Columns: Date / Time, Channel, Staff, Subject, Status. Last 12 shown. | Match | `StaffMessages` component | Columns are Date/Time, Channel, Sent To, Subject, Status (shows last 10 instead of 12) — effectively equivalent. |
| Client/role message composer header | Title changes per role: "Send / Save Client Message" (admin), "Send Work Message" (staff), "Send Message to AL TAX" (client), "Send Payroll Message" (employee). | Partial | `ClientMessages` component | Only the admin/staff variant ("Send / Save Client Message") exists. Client and employee roles get no composer at all in the new app (see next row). |
| Composer availability for client/employee roles | Clients and employees can compose and send their own messages to AL TAX (document questions, payment questions, paystub questions, etc.), selecting from role-specific templates. | Missing | `CommunicationsPage.tsx` (non-manage branch) | Non-admin/staff users only see a read-only list of their own communications (`CommunicationCard` list); there is no send/reply form for clients or employees. |
| Template dropdown | Select populated with role-specific defaults (varies by role) plus any custom templates saved in Templates table; includes a "?" help tooltip. | Partial | `ClientMessages` component | Template dropdown exists (fed by `/templates`) but only for the admin/staff composer; no role-specific default list, and no help tooltip. |
| "Send To" field | Free-text input, defaulted to client email (or "AL TAX SERVICE" for client/employee roles). | Partial | `ClientMessages` component | Present but read-only, always the client's email; no equivalent for client/employee-initiated messages. |
| "SMS / WhatsApp Phone" input | Free-text field, defaulted to client phone. | Missing | — | Not present in `ClientMessages` composer. |
| "SMS Gateway Email" input | Optional carrier gateway email field. | Missing | — | Not present. |
| "Subject" field | Free-text, role-based default subject text. | Match | `ClientMessages` component | Present, default "Client message". |
| "English Message" textarea | Free-text message body. | Match | `ClientMessages` component | Present. |
| "Arabic Message" textarea (RTL) | Free-text Arabic body, right-to-left direction. | Match | `ClientMessages` component | Present (no explicit `dir="rtl"` styling confirmed on the textarea itself, but field exists and is submitted). |
| "Send Email Now?" dropdown | Options: No, Yes (default Yes). | Missing | — | Not present; new app has no send-now/queue distinction in the composer. |
| Channels checkbox grid | Multi-select: Email, Portal Note, SMS, WhatsApp, Phone (Email checked by default), each with a help tooltip. | Partial | `ClientMessages` component | Single-select "Channel" dropdown only (same 5 options), no multi-channel send, no tooltips. |
| Inline process/status note text | Explains what each channel does and how sending confirmation works. | Missing | — | Not present. |
| "Send / Save Client Message" button (label varies by role) | Submits the composed message. | Match | `ClientMessages` component | Present with fixed label "Send / Save Client Message" (does not vary by role since only one composer exists). |
| "Save Portal Note Only" button | One-click shortcut that force-selects only the Portal Note channel and saves without sending email. | Missing | — | Not present; user must manually pick "Portal Note" from the channel dropdown instead. |
| Manual SMS/WhatsApp compose launch | Selecting SMS or WhatsApp opens the device's native SMS/WhatsApp compose window with the message prefilled (`sms:`/`wa.me` links), requiring manual confirmation. | Missing | — | New app just posts to the API; no native compose window is triggered for SMS/WhatsApp. |
| Communication history panel | Header "History" with message count; table columns Date / Time, Direction, Channel, Subject, Status (last 20 shown). | Partial | `ClientMessages` component | History exists (all messages for the client, not capped at 20) but rendered as `CommunicationCard` items (subject, channel, status, date, plus full message body with an English/Arabic toggle) rather than a compact table; no explicit "Direction" column shown. |
| Communication body preview with English/Arabic toggle | Not present in legacy history (legacy only lists subject/status, not full body). | New | `CommunicationCard` in `CommunicationsPage.tsx` | New app shows the full saved message text per entry with a language toggle button — an addition not found in the legacy Communications view. |
| Message Templates admin page | Separate "Templates" view (linked conceptually via the template dropdown) listing all templates with columns Template, Category, Subject, Active, Source, and an "Edit Template"/"Add Template" workflow (subject, English/Arabic body, Active toggle, Notes). | Partial | `TemplatesPage.tsx` (separate page, not reviewed field-by-field here) | A dedicated Templates page exists in the new app and is used by the Communications template dropdown, but it was out of scope for this line-by-line pass — flagged for its own comparison. |

## Reports

| Legacy Item | Description | New App Status | New App Location | Notes |
|---|---|---|---|---|
| Page banner | Eyebrow "Reports", heading = client name (or "Client Reports"), description "Financial statements and client messages are generated from v5 accounting tables for the selected period." | Partial | `ReportsPage.tsx` | Client name heading and a similar description ("Financial statements are generated from general-ledger activity for the selected period.") exist, but only inside the per-client tabs, not as a page-wide banner; no "Client Reports" fallback text. |
| "Print Report" button | Opens a print-preview modal of the current report tab, then auto-triggers the browser print dialog (`window.print()`) after a short delay; modal has Close/Print buttons. | Missing | — | No print button or print-preview modal anywhere in the new Reports page. |
| Report tabs | Tab bar: P&L, Balance Sheet, Payroll, Client Message. | Partial | `ReportsPage.tsx` (`TABS` constant) | New app's tabs are "Firm Overview" (admin only), "P&L", "Balance Sheet" — Payroll and Client Message tabs do not exist; Firm Overview is new (see below). |
| Report panel header | Dynamic title (Balance Sheet / Payroll Dashboard / Client Message / Profit and Loss) with a "Selected client" / period-range subtitle. | Partial | `ReportsPage.tsx` | P&L and Balance Sheet panels show a title and a from–to date subtitle; no dynamic title logic needed since Payroll/Client Message tabs don't exist. |
| P&L: Income section | Line items filtered to non-zero rows, with a "Total Income" row; "No activity in this section for the selected period." empty state. | Match | `ReportsPage.tsx` (P&L tab) | Income rows + "Total Income" bold row present; no explicit empty-state message for Income specifically (only shown for COGS). |
| P&L: Cost of Goods Sold section | Line items + "Total Cost of Goods Sold" row. | Match | `ReportsPage.tsx` (P&L tab) | Present, including the "No activity in this section for the selected period." empty-state message. |
| P&L: Gross Profit row | Computed total (Income − COGS). | Match | `ReportsPage.tsx` (P&L tab) | Present as a bold row. |
| P&L: Expenses section | Line items + "Total Expenses" row. | Match | `ReportsPage.tsx` (P&L tab) | Present. |
| P&L: Net Income row | Computed total (Gross Profit − Expenses), shown as the document's bottom line. | Match | `ReportsPage.tsx` (P&L tab) | Present, styled with accent color. |
| Report document shell (client meta) | Header block with client name, "Client ID: …", "EIN: …", address, report title, and formatted period range, used for print/document framing. | Missing | — | New app shows client name and a plain description line, but no Client ID/EIN/Address metadata block on the report. |
| Balance Sheet: Assets section | Line items + "Total Assets" row. | Match | `ReportsPage.tsx` (Balance Sheet tab) | Present. |
| Balance Sheet: Liabilities section | Line items + "Total Liabilities" row. | Match | `ReportsPage.tsx` (Balance Sheet tab) | Present. |
| Balance Sheet: Equity section | Separate list of posted equity GL accounts + "Posted Equity" line. | Missing | — | New app has no equity-accounts section; equity is only shown as a single computed "Equity (Assets − Liabilities)" row. |
| Balance Sheet: "Current Period Net Income" row | Adds current-period net income into the equity total. | Missing | — | Not shown; new Balance Sheet doesn't fold net income into equity as a separate labeled row. |
| Balance Sheet: "Total Equity" row | Sum of posted equity + current period net income. | Partial | `ReportsPage.tsx` (Balance Sheet tab) | New app shows "Equity (Assets − Liabilities)" instead, a different (residual) calculation rather than a ledger-based equity total. |
| Balance Sheet: "Total Liabilities + Equity" row | Cross-check total for the accounting equation. | Missing | — | Not present. |
| Balance Sheet: "Balance Check" row + footer note | Shows Assets − (Liabilities + Equity), should equal $0.00, with note "Balance Check should be $0.00 when all GL entries are balanced." | Missing | — | No balance-check validation row or note in new app. |
| Payroll Dashboard tab (entire tab) | Full payroll reporting tab — see rows below. | Missing | — | The "Payroll" tab does not exist in the new Reports page at all; only a single "Payroll Gross" metric appears inside the P&L "Period Snapshot" panel. |
| Payroll Dashboard: calculation strip | Metric cards: Gross Wages, Checks, Employee Taxes, Employer Taxes. | Missing | — | Not present (no Payroll tab). |
| Payroll Dashboard: "Net Pay" / "Total Payroll Cost" rows | Computed totals. | Missing | — | Not present. |
| Payroll Dashboard: Payroll Tax Summary table | Columns: Tax Type, Employee, Employer, Total, Payment Date; rows for Federal Withholding, Social Security, Medicare, MD Withholding, MD UI, Form 940/FUTA (year-end only), Total Payroll Tax. | Missing | — | Not present. |
| Payroll Dashboard: payroll check table | Columns: Date, Employee, Gross, Net — one row per paycheck in the period. | Missing | — | Not present. |
| Client Message tab (entire tab) | Full bilingual period-message generator — see rows below. | Missing | — | The "Client Message" tab does not exist in the new Reports page. |
| Client Message: English/Arabic message preview | Auto-generated bilingual summary (sales tax due, payroll figures, important dates, sales tax detail, payroll summary/detail, FUTA/940 if year-end) shown in two preview boxes. | Missing | — | Not present anywhere in new app UI. |
| Client Message: "Save Period Message" button | Saves the generated bilingual summary to the client's portal/communication history as a Portal Note. | Missing | — | Not present. |
| Client Message: "Open Communications to Send" button | Hands the generated message off to the Communications page, pre-filled and ready to choose channels and send. | Missing | — | Not present; no cross-page handoff between Reports and Communications in new app. |
| Period Snapshot panel | Side panel with header "Period Snapshot" / "Selected client". | Match | `ReportsPage.tsx` (P&L tab, right column) | Present. |
| Period Snapshot: calculation strip | Metric cards: Sales Tax, Payroll Gross, Net Income, GL Entries. | Match | `ReportsPage.tsx` (P&L tab) | All four metrics present with matching labels. |
| Period Snapshot: diagnostic notes box | Shows accounting diagnostic warnings/notes when present (e.g., data-quality flags). | Missing | — | No diagnostic/warning box in new app. |
| Period Snapshot: Account/Debit/Credit table | Full GL account listing with debit and credit totals per account. | Match | `ReportsPage.tsx` (P&L tab) | Present with the same three columns. |
| Period selector (global Period Start/End date filter) | A shared top-of-app date range filter (Period Start / Period End) that drives all report calculations. | Partial | `ReportsPage.tsx` (From/To date inputs) | New app has its own local "From"/"To" date pickers scoped to the Reports page (defaulting to first-of-month through today) rather than a single global filter shared across the whole app. |
| Firm Overview tab (revenue/expense/profit trend, unpaid balance, active clients) | No equivalent tab exists inside legacy `renderReports()`; a similar firm-wide summary only appears as small metric tiles ("Active Clients", "Unpaid Balance", etc.) on the main Dashboard/summary bar shown across all views. | New | `ReportsPage.tsx` ("Firm Overview" tab, admin only) | New app adds a dedicated "Firm Overview" tab with Revenue/Expenses/Net Profit/Unpaid Balance metric cards and a 6-month Monthly Trend table (Month, Revenue, Expenses, Profit) plus active-client and unpaid-invoice counts — this consolidates data that in the legacy app was scattered across the dashboard summary bar rather than presented as a report. |

# Parity Audit — Chunk 1 (Code.gs lines 1–2000)

**Total functions audited: 72**
FULL: 11 · PARTIAL: 3 · MISSING: 2 · N/A-INTERNAL: 31 · N/A-DEAD: 3 · N/A-SHEET-ONLY: 22

| Line | Function | Purpose | Verdict | Evidence |
|---|---|---|---|---|
| 189 | `alTaxV5IsRestrictedDataTable_` | Checks whether a sheet/table name is in the restricted set (`v3_Client_Secrets`, `v3_Secret_Access_Log`). | N/A-INTERNAL | Supports secret-table access gating. Concept is covered in the new app by `src/modules/vault/vault.routes.ts:50` (`vaultRouter.use(requireAuth, requireRole("admin"))`) plus a dedicated `v3_secret_access_log` audit insert. |
| 196 | `onOpen` | Builds the "AL TAX Tools" custom Spreadsheet menu (setup/repair/trigger items). | N/A-SHEET-ONLY | Google Sheets UI menu wiring; no meaning in a real app. |
| 231 | `alTaxV3SetupSheets` | Creates/repairs the v3 schema tabs with header rows and formatting. | N/A-SHEET-ONLY | Sheet-tab bootstrap mechanics; new app uses `sql/schema_source.json` + migrations instead. |
| 249 | `alTaxV3SystemCheck` | Writes a `v3_System_Check` review sheet listing missing columns per table. | N/A-SHEET-ONLY | Spreadsheet schema-diagnostic tool. |
| 276 | `v3HasCoreData_` | Returns true if `v3_Clients`/`v3_Tasks` have data — used as the v2-vs-v3 data-source switch throughout the file. | N/A-INTERNAL | Supports `getAdminData`/`getClientPortalData` dispatch (see rows below). New app is v3-schema only (`sql/schema_source.json` has only `v3_*` tables), so this switch has no counterpart need. |
| 283 | `v3Sheet_` | Gets a named sheet or throws if missing. | N/A-INTERNAL | Generic sheet-access helper. |
| 289 | `v3DeleteRow_` | Deletes a specific row from a named v3 sheet. | N/A-INTERNAL | Generic row-delete helper (not directly exercised as a user feature in this line range). |
| 298 | `v3Headers_` | Reads a sheet's header row as trimmed strings. | N/A-INTERNAL | Sheet-schema helper. |
| 305 | `v3HeaderIndex_` | Finds a header's column index. | N/A-INTERNAL | Sheet-schema helper. |
| 309 | `v3ReadRows_` | Reads all data rows of a v3 sheet into header-keyed objects. | N/A-INTERNAL | Generic "read table" helper; new app equivalent is just `SELECT * FROM altax.v3_x`. |
| 324 | `v3ReadRowObject_` | Reads a single row by row number into an object. | N/A-INTERNAL | Generic "read one row" helper. |
| 337 | `v3AppendObject_` | Appends a new row from a field-keyed object. | N/A-INTERNAL | Generic "insert row" helper; new app equivalent is a parameterized `INSERT`. |
| 347 | `v3UpdateFields_` | Updates specific named fields of a row. | N/A-INTERNAL | Generic "update row" helper. |
| 358 | `v3DateDisplay_` | Formats a Date value as `MM/dd/yyyy` for display, else empty string. | N/A-INTERNAL | Formatting helper. |
| 365 | `v3Money_` | Coerces a value to a number, defaulting to 0. | N/A-INTERNAL | Formatting/parsing helper. |
| 370 | `v3EscapeHtml_` | Escapes `&<>"'` for safe HTML interpolation. | N/A-INTERNAL | Used by HTML-generation functions (e.g. statement PDF). New app uses a templating/PDF library instead of string concatenation, so no literal counterpart is needed. |
| 382 | `v3ClientMap_` | Builds a `ClientID -> client row` lookup map. | N/A-INTERNAL | Generic join helper; new app does this with SQL joins. |
| 391 | `v3UserAliasesForEmail_` | Builds a set of name/userID/email aliases for a user, used to match "AssignedTo" free-text fields. | N/A-INTERNAL | Supports `v3GetStaffPortalData`'s "my tasks" filter. New app equivalent: `getUserAliases` in `src/common/assignment.ts`, used by `src/modules/communications/communications.routes.ts` and task assignment checks. |
| 405 | `v3LogAudit_` | Appends a row to `v3_Audit_Log` (best-effort, swallows errors). | N/A-INTERNAL | New app equivalent: `logAudit()` in `src/common/audit.ts`, called from nearly every mutating route (e.g. `src/modules/billing/billing.routes.ts:268`). |
| 424 | `alTaxFullSetup` | Menu action that runs all the sheet-repair functions plus a system check. | N/A-SHEET-ONLY | Orchestrates only sheet-repair menu items (rows 26–39 below). |
| 436 | `sh_` | Gets a named sheet or throws. | N/A-INTERNAL | Duplicate of `v3Sheet_` pattern for non-v3 sheets. |
| 443 | `maybeSh_` | Gets a named sheet or returns null. | N/A-INTERNAL | Sheet-existence helper. |
| 447 | `safeBreakApart_` | Un-merges any merged cells in a range before writing to it. | N/A-SHEET-ONLY | Spreadsheet cell-merge mechanics. |
| 453 | `setRichLink_` | Writes a styled rich-text hyperlink into a cell (used for nav links). | N/A-SHEET-ONLY | Spreadsheet cell formatting/linking. |
| 474 | `alTaxAddNavigation` | Adds HOME/BACK/NEXT link cells to every sheet tab. | N/A-SHEET-ONLY | Spreadsheet cross-tab navigation; replaced by the app's React Router nav (unrelated mechanism, not a per-function port). |
| 497 | `alTaxRebuildLists` | Rebuilds the `Lists` sheet (dropdown source values: views, months, employees, statuses, payment methods). | N/A-SHEET-ONLY | Spreadsheet data-validation source lists. |
| 551 | `addEmployeeName_` | Filters/cleans a raw cell value before adding it to the employee-name set. | N/A-SHEET-ONLY | Only supports `alTaxRebuildLists`. |
| 561 | `alTaxApplyDropdowns` | Applies data-validation dropdown rules to various sheet cells. | N/A-SHEET-ONLY | Spreadsheet data-validation wiring. |
| 595 | `periodStartFormula_` | Builds a spreadsheet formula string computing a period's start date. | N/A-SHEET-ONLY | Spreadsheet formula generator. |
| 601 | `periodEndFormula_` | Builds a spreadsheet formula string computing a period's end date. | N/A-SHEET-ONLY | Spreadsheet formula generator. |
| 607 | `allEmployeesTest_` | Builds a formula fragment testing for the "All Employees" filter value. | N/A-SHEET-ONLY | Spreadsheet formula fragment. |
| 611 | `totalWagesFormula_` | Builds a `SUMIFS` formula for total wages by employee/period. | N/A-SHEET-ONLY | Spreadsheet formula generator. |
| 617 | `employeeCountFormula_` | Builds a formula counting unique employees paid in a period. | N/A-SHEET-ONLY | Spreadsheet formula generator. |
| 627 | `peSumFormula_` | Builds a `SUMIFS` formula against the `Payroll_Engine` sheet for one column. | N/A-SHEET-ONLY | Spreadsheet formula generator. |
| 633 | `alTaxRepairClientMessage` | Writes payroll-summary formulas into the `Client_Message` sheet. | N/A-SHEET-ONLY | Spreadsheet cell-formula repair tool. |
| 677 | `alTaxRepairPayrollDashboard` | Writes payroll-summary formulas into the `Payroll_Dashboard` sheet. | N/A-SHEET-ONLY | Spreadsheet cell-formula repair tool. |
| 692 | `alTaxRepairReports` | Writes period-selector formulas into `Print_PnL`/`Print_Balance_Sheet`/`Statement_of_Account` sheets. | N/A-SHEET-ONLY | Spreadsheet cell-formula repair tool. |
| 706 | `alTaxClearTestRows` | Removes rows containing "TEST" from `Payroll_Input`/`Employee_Master`, then rebuilds Lists. | N/A-SHEET-ONLY | Spreadsheet test-data cleanup utility; a real DB app doesn't need this class of maintenance tool. |
| 734 | `alTaxSystemCheck` | Writes a `Workbook_Review` sheet with pass/check test rows for key formulas. | N/A-SHEET-ONLY | Spreadsheet QA/test-notes tool. |
| 767 | `onEdit` | Simple trigger: on editing a `v2_`-prefixed sheet, auto-generates an ID, sets created/modified timestamps and user stamps. | N/A-SHEET-ONLY | Google Sheets `onEdit` trigger mechanics specific to the legacy `v2_` tabs (which have no counterpart — new app schema is v3-only per `sql/schema_source.json`). The underlying *concept* of auto-generated IDs and audit timestamps is carried forward differently, e.g. `nextInvoiceId()`/`nextPaymentId()` in `src/modules/billing/billing.routes.ts:52-56` and `updated_at = now()` on every UPDATE. |
| 800 | `doGet` | Web-app GET entry point: resolves a `portal`/`role` query param to the matching Login_* HTML template (with HTML-output fallbacks). | FULL | New app replaces per-role static login pages with a single SPA login screen offering a role picker: `frontend/src/pages/LoginPage.tsx` (`PORTALS` list, `portal` state, `login(email, portal, password)`), served by the Express/Vite app instead of `HtmlService`. |
| 833 | `alTaxCurrentPortalRoleFromRequest_` | Extracts the `portal`/`role`/`p` query param from the doGet request. | N/A-INTERNAL | Supports `doGet`'s role dispatch only. |
| 844 | `alTaxNormalizePortalRole_` | Normalizes a raw role string to `admin/staff/client/employee/general`. | N/A-INTERNAL | Supports `doGet`'s role dispatch only. |
| 850 | `alTaxLoginFileForPortal_` | Maps a normalized role to its Login_* HTML filename. | N/A-INTERNAL | Supports `doGet`'s role dispatch only. |
| 859 | `alTaxPortalTitleForRole_` | Maps a normalized role to a page `<title>`. | N/A-INTERNAL | Supports `doGet`'s role dispatch only. |
| 868 | `includePortalIndex` | Public wrapper calling `alTaxPortalHtmlForRole_`. | N/A-INTERNAL | Thin HtmlService templating wrapper. |
| 872 | `alTaxPortalHtmlForRole_` | Injects a role-lock `<script>` block into the `Index.html` content. | N/A-INTERNAL | GAS HtmlService string-injection mechanic; no counterpart needed since the SPA passes role as normal client-side state (`LoginPage.tsx`). |
| 890 | `alTaxEscapeHtml_` | HTML-escapes a value (duplicate of `v3EscapeHtml_`). | N/A-INTERNAL | Duplicate formatting helper. |
| 899 | `alTaxV16InstallErrorHtml_` | Builds a static HTML "install check" error page shown if `Index`/login HTML files fail to load in Apps Script. | N/A-DEAD | Purely a GAS-deployment diagnostic page for when the Apps Script project's own file set is broken; meaningless in a compiled/deployed Node+React app where this failure mode can't occur. |
| 911 | `authenticateUser` | Legacy v2 login: looks up a user/client by email in `v2_Users`/`v2_Clients`, checks active status, resolves role (admin override for `altax@almabarigroup.com`, admin/staff/client roles, direct client login by primary email). | FULL | `src/modules/auth/auth.service.ts` `authenticateUser()` (used by `POST /auth/login` in `src/modules/auth/auth.routes.ts:43`) re-implements the same role/active-status/client-lookup logic against `v3_users`/`v3_clients`, and adds password hashing (`password.ts`) and TOTP 2FA (`totp.ts`) on top — a strict superset, not a gap. |
| 1013 | `v3TaskToPipeline_` | Maps a v3 task row + client map to a UI "pipeline" object (client name, service line, deadline, etc.). | N/A-INTERNAL | Row-shaping helper for `v3GetAdminData`/`v3GetStaffPortalData`. |
| 1030 | `v3InvoiceToAdmin_` | Maps a v3 invoice row + client map to an admin-view invoice object. | N/A-INTERNAL | Row-shaping helper for `v3GetAdminData`. |
| 1049 | `v3DocumentRequestToAdmin_` | Maps a v3 document-request row + client map to an admin-view object. | N/A-INTERNAL | Row-shaping helper for `v3GetAdminData`. |
| 1064 | `v3ReadTemplates_` | Reads `Messaging_Templates` sheet (name, body) into a list. | FULL | `src/modules/templates/templates.routes.ts` `GET /` (line 33) — richer than legacy: adds `category`, `subject`, `active`, and a built-in-vs-custom-override source, on top of name/body. |
| 1077 | `v3ReadChecklist_` | Reads the single global `Month_End_Checklist` sheet (category, task, status, notes, date) into a list. | FULL | `src/modules/accounting/monthEndChecklist.ts` `GET /:clientId` (line 47) — redesigned to be per-client + per-period (`YYYY-MM`) with a merged standard-items view, rather than one firm-wide checklist; the checklist *feature* itself is present and more granular. Note: that file's own comment says "no legacy equivalent found," which is inaccurate — `v3ReadChecklist_`/`Month_End_Checklist` is the direct predecessor, just restructured. |
| 1098 | `v3Analytics_` | Computes dashboard analytics: revenue/expenses/profit/taxLiabilities from static `Dashboard_Data` cells, plus unpaidInvoices summed live from `v3_Invoices`. | PARTIAL | `src/modules/reports/reports.routes.ts` `GET /firm-summary` (line 24) computes revenue/expenses/profit live from `v3_gl_entries` (an improvement over legacy's static cell refs) and unpaid balance/count from `v3_invoices`, and adds `activeClientCount` — but it does **not** return a `taxLiabilities` figure anywhere in the response (checked lines 24-74; response fields are `months`, `totals`, `unpaidBalance`, `unpaidInvoiceCount`, `activeClientCount`). |
| 1116 | `v3GetAdminData` | Aggregates checklist, clients, templates, analytics, master task pipeline, invoices, documents, and staff list into one payload for the admin dashboard. | FULL | New app has no single aggregate endpoint; the SPA composes the equivalent view from separate REST calls — confirmed in `frontend/src/pages/DashboardPage.tsx:122-125` (`api.get("/tasks")`, `/clients`, `/documents/requests`, `/billing/invoices`), plus `/users`, `/templates`, `/accounting/month-end-checklist`, `/reports/firm-summary` elsewhere. Each underlying data slice is independently confirmed FULL/PARTIAL above. |
| 1172 | `v3GetClientPortalData` | Aggregates a client's invoices, document requests, and active tasks, plus totals owed, for the client portal home. | FULL | Composed client-side from `GET /billing/invoices` (role-scoped to the client, `billing.routes.ts:116`), `GET /documents/requests`, and `GET /tasks` (role-scoped) rather than one function — same data, REST-composed. |
| 1233 | `v3GetStaffPortalData` | Aggregates a staff member's assigned tasks (matched via alias set) and the client list for the staff portal home. | FULL | Composed from `GET /tasks` (assignment-filtered per `src/common/assignment.ts`) and `GET /clients`. |
| 1264 | `v3CreateTaskRecord_` | Appends a new row to `v3_Tasks` with default status/audit fields. | N/A-INTERNAL | Private write-helper (caller is beyond line 2000 in this file). Feature-level equivalent: `POST /` in `src/modules/tasks/tasks.routes.ts:82`. |
| 1293 | `v3CreateClientRecord_` | Appends a new row to `v3_Clients` with default enabled flags. | N/A-INTERNAL | Private write-helper (caller is beyond line 2000). Feature-level equivalent: `POST /` in `src/modules/clients/clients.routes.ts:109`, which additionally rejects duplicate client names (409) — legacy has no such guard. |
| 1324 | `v3CreateUserRecord_` | Appends a new row to `v3_Users` with default active flag. | N/A-INTERNAL | Private write-helper (caller is beyond line 2000). Feature-level equivalent: `POST /` in `src/modules/users/users.routes.ts:68`. |
| 1342 | `v3UpdateInvoiceDetails` | Edits an invoice's description/amount/dueDate; recomputes balance/status; blocks lowering amount below what's already paid. | FULL | `PATCH /invoices/:invoiceId` in `src/modules/billing/billing.routes.ts:243` — same recompute pattern, plus adds an access-control check (`canMutateInvoice`) legacy lacked. |
| 1365 | `v3UpdateInvoiceStatus` | Sets invoice status with rule-based paid/balance recompute: Paid→paid=total; Unpaid/Open→paid=0; Void→paid=0,balance=0; **Partial is rejected unless `0 < paid < total` already** ("Record a partial payment before setting this invoice to Partial."). | PARTIAL | `PATCH /invoices/:invoiceId` (`billing.routes.ts:243-270`) lets the caller set any `status` string directly and only recomputes balance from `total`/`paid` if those are also passed in the body — it does **not** enforce the "Partial requires an existing partial payment" guard, and does not force paid=0 when status is set to Unpaid/Open. A caller could PATCH `status=Partial` with no payment recorded (paid stays 0, balance stays total), which legacy explicitly blocked. (`POST /invoices/:invoiceId/void`, line 277, does correctly replicate the Void branch as its own endpoint.) |
| 1396 | `v3RecordInvoicePayment` | Records a manual payment against an invoice; caps payment so total paid can't exceed invoice total; recomputes status. | FULL | `POST /invoices/:invoiceId/payments` in `billing.routes.ts:299` — same over-payment cap and status recompute, plus payment-method bank detail resolution and access control. |
| 1439 | `v3GetInvoicePayments` | Lists payments recorded against one invoice. | FULL | `GET /invoices/:invoiceId/payments` in `billing.routes.ts:154`, with added role-based visibility check (client sees only their own). |
| 1458 | `v3ReverseInvoicePayment` | Reverses a payment (requires a reason), rolls back the invoice's paid/balance/status, marks the payment `Status=Reversed`. | FULL | `POST /payments/:paymentId/reverse` in `billing.routes.ts:415` — same required-reason guard, already-reversed guard, and rollback math (verified lines 415-422+). |
| 1509 | `v3DispatchInvoiceCommsAction` | Sends an invoice-due email via `GmailApp`, or for SMS returns `{phone, message}` for the client-side to hand off to a phone's SMS app. | MISSING | `src/modules/communications/communications.routes.ts:17-20` explicitly documents: "Deliberately NOT ported... No MailApp/SMS gateway sending anywhere (no email/SMS infra exists in this backend). Every communication is saved with Status='Saved'; legacy's 'sendNow' email/SMS branches are skipped entirely." Checked `billing.routes.ts` and `communications.routes.ts` in full — no email/SMS dispatch of any kind exists. |
| 1537 | `v3DispatchStaffCommsAction` | Sends a "check your portal" alert email to a staff member via `GmailApp`, or returns `{phone, message}` for SMS. | MISSING | Same as above — `communications.routes.ts:17-20` confirms no email/SMS sending exists anywhere in the new backend; only a communications *log* is written. |
| 1559 | `v3GenerateStatementOfAccount` | Builds a client statement-of-account HTML→PDF, saves it into a Drive folder (`AL_TAX_Client_Uploads/{clientId} - {clientName}`), shares it view-only, and returns a permanent `fileUrl`. | PARTIAL | `GET /clients/:clientId/statement` in `billing.routes.ts:199` + `src/modules/billing/statementPdf.ts` generates an equivalent PDF (invoices, totals, balance) with a date-range filter (`?start=&end=`) legacy lacks — but it streams the PDF back on each request rather than persisting/storing a file; there is no Drive-equivalent storage and no `v3_Statements`-style table in `sql/schema_source.json`, so unlike legacy there's no durable, shareable statement link a client can be sent and reopen later. |
| 1645 | `getClientPortalData` | Dispatcher: if v3 core data exists, delegates to `v3GetClientPortalData` (see above, FULL); otherwise falls back to reading legacy `v2_Billing_Invoices`, `Portal_View`, `v2_Tax_Returns`, `v2_Compliance_Events`, `v2_Payroll_Runs` sheets directly. | N/A-DEAD | The v3 delegation path is already covered above. The v2-fallback branch reads a schema (`v2_*` sheets, `Portal_View`) that does not exist anywhere in the new app — `sql/schema_source.json` contains only `v3_*` tables — confirming this fallback path is legacy-only dead weight superseded by v3 well before this rewrite. |
| 1814 | `getAdminData` | Dispatcher: if v3 core data exists, delegates to `v3GetAdminData` (see above, FULL); otherwise falls back to reading legacy `v2_Users`, `v2_Clients`, `v2_Billing_Invoices`, `Portal_View`, `Month_End_Checklist`, `v2_Tax_Returns`, `v2_Compliance_Events`, `v2_Payroll_Runs` sheets directly (function body continues past line 2000). | N/A-DEAD | Same reasoning as `getClientPortalData`: v3 path already covered; the v2-fallback branch's source tables have no counterpart in `sql/schema_source.json` (v3-only schema), confirming it as superseded legacy fallback code. |

# Parity audit — Code.gs lines 7739–9656 (chunk 3)

**Total functions audited: 85**
FULL: 39 | PARTIAL: 14 | MISSING: 20 | N/A-INTERNAL: 11 | N/A-DEAD: 0 | N/A-SHEET-ONLY: 1

This range covers: password hashing/verification, login-lockout, the legacy mandatory
email-OTP second-factor flow, invite-token lifecycle, portal role resolution/authentication
(`alTaxV3AuthenticateUser`), the monolithic per-role portal data filter
(`alTaxV3PortalFilterData_` and its table-list helpers), client/document/communication
visibility predicates, bilingual (English/Arabic) communication helpers, and the
task-management portal actions (create/update/void/delete/bulk-complete/bulk-delete).

| Line | Function | Purpose | Verdict | Evidence |
|---|---|---|---|---|
| 7753 | `alTaxWebSafeValue_` | Converts a Sheet cell value (Date/null) to a JSON-safe value for web responses. | N/A-INTERNAL | Generic serialization helper for `alTaxWebSafeRows_`; superseded by pg driver/JSON.stringify auto date handling. |
| 7763 | `alTaxWebSafeRows_` | Reads a sheet's rows and redacts sensitive `v3_Users` fields (PasswordHash/InviteToken/PasswordSalt) before sending to the web client. | FULL | src/modules/users/users.routes.ts GET `/` — never selects password_hash/password_salt/invite_token at all, substitutes `has_pending_invite` boolean (stricter than legacy's masking). |
| 7785 | `getUnifiedWebAppData` | Returns ALL non-restricted tables' rows, unfiltered by role/client — a full data dump. | MISSING | Checked all route files under src/modules/**; every list/GET endpoint applies role-based scoping (requireRole/canAccessClient/canAccessTask). No unscoped "dump everything" endpoint exists anywhere in the new app. |
| 7800 | `alTaxV3NormalizeText_` | trim+lowercase string normalizer. | FULL | src/common/assignment.ts `normalizeText()`; also reimplemented inline in src/modules/auth/auth.service.ts. |
| 7804 | `alTaxV5RuleTriggerColumnKey_` | Normalizes a task-rule trigger column name, special-casing "MDWithholdingFrequency". | N/A-INTERNAL | Helper only used by `alTaxV5EnsureQuarterlyMDWithholdingRule_` (see line 9023, MISSING — deliberately not ported). |
| 7810 | `alTaxV3IsActive_` | Returns false for status values no/false/inactive/archived. | FULL | src/modules/auth/auth.service.ts `isActive()`, identical logic. |
| 7815 | `alTaxV5HashPassword_` | Legacy single-round unsalted SHA-256 password hash. | FULL | src/modules/auth/password.ts `legacyHash()` — file header states "Ported directly from ... alTaxV5HashPassword_". |
| 7830 | `alTaxV5DigestHex_` | SHA-256 hex digest helper. | FULL | src/modules/auth/password.ts `digestHex()`. |
| 7838 | `alTaxV5NewPasswordSalt_` | Generates a random salt from two UUIDs. | FULL | src/modules/auth/password.ts `newSalt()`, same construction (`crypto.randomUUID()` x2). |
| 7842 | `alTaxV5IteratedPasswordHash_` | 12,000-round salted SHA-256 KDF. | FULL | src/modules/auth/password.ts `iteratedHash()`, identical algorithm and default iteration count (`PASSWORD_HASH_ITERATIONS = 12000`). |
| 7853 | `alTaxV5CreatePasswordHash_` | Builds the "v2$iterations$salt$hash" password hash string. | FULL | src/modules/auth/password.ts `createPasswordHashFields()` inline hash construction, same format string. |
| 7861 | `alTaxV5PasswordHashFields_` | Wraps hash creation into the field-set written to a user row. | FULL | src/modules/auth/password.ts `createPasswordHashFields()`, same field shape (PasswordHash/PasswordSalt/PasswordHashVersion/LastPasswordChangeAt/FailedLoginCount/LockedUntil). |
| 7874 | `alTaxV5VerifyPassword_` | Verifies a password against either hash format, flags legacy hashes for upgrade. | FULL | src/modules/auth/password.ts `verifyPassword()`, same two-format check and `needsUpgrade` flag. |
| 7891 | `alTaxV5EnsureUserSecurityColumns_` | Ensures v3_Users sheet has the security columns (cached check), Apps-Script schema-drift mechanic. | N/A-SHEET-ONLY | Real app manages schema via SQL migrations (sql/001_init_schema.sql), not runtime column-existence checks. |
| 7903 | `alTaxV5LoginLockedUntilMs_` | Reads LockedUntil as epoch ms. | FULL | src/modules/auth/auth.service.ts inline `row.locked_until ? new Date(row.locked_until).getTime() : 0`. |
| 7911 | `alTaxV5FormatDateTime_` | Formats a date for lockout error messages. | FULL | src/modules/auth/auth.service.ts uses `new Date(lockedUntilMs).toLocaleString()` in the same error strings. |
| 7917 | `alTaxV5ResetLoginFailures_` | Clears FailedLoginCount/LockedUntil on successful login. | FULL | src/modules/auth/auth.service.ts inline `UPDATE altax.v3_users SET failed_login_count = NULL, locked_until = NULL ...`. |
| 7929 | `alTaxV5RecordFailedLogin_` | Increments failure count, locks account after 5 failures for 15 minutes. | FULL | src/modules/auth/auth.service.ts `recordFailedLogin()` — same LOGIN_FAILURE_LIMIT=5 / LOGIN_LOCK_MINUTES=15 constants, same audit log call. |
| 7949 | `alTaxV5NewInviteToken_` | Generates a random invite token (UUID + 6-digit suffix). | FULL | src/modules/users/users.routes.ts `newInviteToken()`, identical construction. |
| 7953 | `alTaxV5PortalRoleForInvite_` | Trivial wrapper around role normalization for invite context. | N/A-INTERNAL | Callers use `normalizePortalRole()` directly (auth.service.ts); no separate wrapper needed. |
| 7957 | `alTaxV5PortalBaseUrl_` | Resolves the portal's base URL from script properties or deployment URL. | FULL | src/modules/users/users.routes.ts `inviteLink()` uses `process.env.FRONTEND_BASE_URL`. |
| 7969 | `alTaxV5PortalInviteLink_` | Builds a portal login link with role+token query params. | FULL | src/modules/users/users.routes.ts `inviteLink()` — doc comment explicitly says "Mirrors alTaxV5PortalInviteLink_", points at `/accept-invite` instead. |
| 7981 | `alTaxV5PortalInviteLinkText_` | Fallback text if no link can be built. | N/A-INTERNAL | Trivial fallback string, folded into `inviteLink()`'s always-available env-based URL. |
| 7985 | `alTaxV5TrySendInviteEmail_` | Emails the invite link/token via MailApp. | MISSING | src/modules/users/users.routes.ts doc comment: "This backend has no email-sending infrastructure yet ... the invite link is returned in the response for the admin to deliver manually." |
| 8013 | `alTaxV5MaskEmail_` | Masks an email for display during 2FA (`ab***@x***.com`). | MISSING | Only used by the legacy mandatory email-OTP flow, which is not ported (see totp.ts doc comment at lines 26–30 below) — replaced by optional TOTP with no masked-email display. |
| 8027 | `alTaxV5LoginChallengeKey_` | Cache key for a pending login 2FA challenge. | MISSING | Part of the not-ported email-OTP flow; new app's 2FA challenge is a signed JWT (auth.routes.ts), not a cache-key lookup. |
| 8031 | `alTaxV5NewLoginCode_` | Generates a random 6-digit login code. | MISSING | Not ported — email-OTP flow replaced by TOTP (`generateTotpSecret`/`verifyTotpCode` in src/modules/auth/totp.ts). |
| 8035 | `alTaxV5CreateLoginChallenge_` | Creates and caches a 2FA challenge (code hash, 10-min expiry). | MISSING | New app's equivalent step (src/modules/auth/auth.routes.ts `/login`) issues a signed JWT "2fa-challenge" only when TOTP is enabled — no code-hash cache, no email dispatch. |
| 8056 | `alTaxV5ClearLoginChallenge_` | Removes a login challenge from cache. | MISSING | Same reasoning — no cache-based challenge exists to clear. |
| 8062 | `alTaxV5SendLoginCode_` | Emails the 6-digit verification code via MailApp. | MISSING | No email infra (see 7985); TOTP codes come from the user's own authenticator app, nothing to send. |
| 8088 | `alTaxV5VerifyLoginChallenge_` | Verifies the submitted 2FA code against the cached challenge. | MISSING | Replaced by `/auth/login/verify-totp` (auth.routes.ts) — different mechanism (TOTP, optional per-user) not a like-for-like port; every login-mandatory-2FA function in this cluster (8013–8088) has no equivalent, since 2FA is opt-in in the new app. |
| 8140 | `alTaxV5CreateAdminPasswordToken` | Spreadsheet-menu (`ui.prompt`/`ui.alert`) tool to bootstrap/repair the Admin account with an invite token. | MISSING | Checked src/modules/users/users.routes.ts, src/modules/system/system.routes.ts, and searched the repo for any seed/bootstrap script (`find ... -iname "*seed*"`, `*bootstrap*`) — none exists. All admin-management routes require `requireRole("admin")`, so there is no way to create the very first Admin account without direct DB access. |
| 8237 | `alTaxV5SetAdminTemporaryPassword` | Spreadsheet-menu tool to set/reset a temporary Admin password. | MISSING | Same gap as above — src/modules/users/users.routes.ts `/:userId/temporary-password` requires an already-authenticated admin caller, so it cannot bootstrap the first admin. |
| 8329 | `alTaxV3FindClientByEmail_` | Looks up a client company by email, with inactive/portal-disabled diagnostics. | MISSING | src/modules/auth/auth.service.ts `authenticateUser()` final fallback only returns a generic "No matching portal user or client email was found." — it does not separately query v3_clients by email to produce the specific "client exists but no portal account" message. |
| 8349 | `alTaxV3FindEmployeeByEmail_` | Looks up an employee by email, returning an inactive-profile error if Status isn't active. | PARTIAL | src/modules/auth/auth.service.ts `findEmployeeByEmail()` does the lookup but has no active/Status check — an inactive employee's login would not be rejected with "This employee profile is inactive." |
| 8366 | `alTaxV3FindEmployeeById_` | Looks up an employee by ID (no active check in legacy either). | FULL | src/modules/auth/auth.service.ts `findEmployeeById()`, same no-status-check behavior — consistent port. |
| 8376 | `alTaxV3NormalizePortalRole_` | Normalizes a role string; also maps "owner"/"manager" → "admin". | PARTIAL | src/modules/auth/auth.service.ts `normalizePortalRole()` handles admin/staff/client/employee but has no owner/manager→admin aliasing; unrecognized input returns `"general"` instead of legacy's `""`. |
| 8383 | `alTaxV3PortalRoleCacheKey_` | Cache key builder for "last portal role used" memory. | N/A-INTERNAL | Supports the not-ported role-memory cache (see 8387/8396). |
| 8387 | `alTaxV3RememberPortalRole_` | Caches which portal role an email last logged in as (21600s). | MISSING | No CacheService-equivalent "remember last role" mechanism found; new app requires the frontend to pass `portal` explicitly on every `/auth/login` call. |
| 8396 | `alTaxV3LastPortalRole_` | Reads the cached last-used portal role. | MISSING | Same as above — no equivalent found. |
| 8406 | `alTaxV3ExtractEmailRole_` | Parses an `email::portal-role=xxx` marker embedded in the email string. | FULL | Not needed in new app: src/modules/auth/auth.routes.ts `/login` already takes `{ email, portal, password }` as separate fields — the workaround this parses for is unnecessary by design, so the underlying capability (associate a role with a login attempt) is served directly. |
| 8417 | `alTaxV3AuthenticateUser` | Core authentication: per-role active-user match, password verify, lockout, legacy-hash upgrade, client/employee resolution. | FULL | src/modules/auth/auth.service.ts `authenticateUser()` — doc comment: "Ported from alTaxV3AuthenticateUser ... Preserves: per-role active-user matching, 5-attempt/15-minute lockout ... legacy-hash upgrade ... client/employee resolution". |
| 8582 | `authenticateUser` (public, email-only wrapper) | Thin wrapper calling the authenticator with no role/password. | N/A-INTERNAL | No direct analog needed; new app's service layer calls `authenticateUser()` directly with explicit args from each route. |
| 8586 | `alTaxV3UserAliases_` | Builds the set of identity strings (email/name/UserID) a task's AssignedTo may match for a user. | FULL | src/common/assignment.ts `getUserAliases()` — doc comment: "Mirrors alTaxV3UserAliases_". |
| 8608 | `alTaxV3AssignedToUser_` | Checks whether a record's AssignedTo matches one of the user's aliases. | FULL | src/common/assignment.ts `isAssignedToUser()` — doc comment: "Mirrors alTaxV3AssignedToUser_". |
| 8615 | `alTaxV5SafeStaffRecipientUserRow_` | Strips a user row down to a safe subset (name/email/role/etc.) for staff-directory display. | FULL | src/modules/communications/communications.routes.ts GET `/staff-directory` selects exactly `name, email, role` for active admin/staff users. |
| 8632 | `alTaxV5StaffRecipientRowsForPortal_` | Filters v3_Users to unique, active, assignable staff/admin rows for the portal's recipient picker. | FULL | src/modules/communications/communications.routes.ts `/staff-directory` — doc comment: "powers the Firm Staff Messages recipient picker", same active+role filter. |
| 8644 | `alTaxV5IsClientVisibleDocumentRequest_` | Determines if a document request should be shown to the client (excludes void/deleted/internal/staff-only). | FULL | src/modules/documents/documents.routes.ts `isClientVisibleRequest()`, used in GET `/requests` and GET `/requests/:id` for client role. |
| 8656 | `alTaxV5IsClientVisibleDocumentUpload_` | Determines if an uploaded document should be shown to the client. | FULL | src/modules/documents/documents.routes.ts `isClientVisibleUpload()`, used in GET `/uploads` for client role. |
| 8669 | `alTaxV5IsClientVisibleCommunication_` | Determines if a communication row should be shown to the client (excludes internal/staff/task-only). | FULL | src/modules/communications/communications.routes.ts client-role branch (`role === "client"` query filtered to that client's own communications); module doc comment references client-visibility rules directly. |
| 8685 | `alTaxV5IsFirmInvoicePaymentRow_` | Distinguishes a firm-invoice payment row from a tax-payment row (by PAY-T- prefix / InvoiceID presence). | N/A-INTERNAL | New schema's `v3_payments` rows are always invoice-linked (src/modules/billing/billing.routes.ts) — the firm-vs-tax distinction this predicate exists for no longer applies; superseded by schema design. |
| 8691 | `alTaxV5IsClientTaxPaymentRow_` | Identifies a client tax-due payment (quarterly estimate, sales/payroll tax, EFTPS) tracked without an invoice. | MISSING | Checked src/modules/billing/billing.routes.ts and src/modules/accounting/accounting.routes.ts — payments are always tied to an invoice_id; no route or table tracks a standalone client tax-liability payment record. |
| 8713 | `alTaxV5PaymentOpenAmount_` | Computes `max(expected - paid, 0)` remaining balance on a payment. | FULL | src/modules/billing/billing.routes.ts uses the equivalent `Math.max(0, total - paid)` pattern repeatedly for invoice balance_due. |
| 8720 | `alTaxV5NormalizeClientLanguagePreference_` | Normalizes a client's language preference to Arabic/English/Both. | FULL | src/modules/communications/communications.routes.ts `normalizeLanguagePreference()`, same three-way logic including Arabic-script substring match. |
| 8727 | `alTaxV5ArabicDocumentItem_` | Translates common document-item names (bank statement, IRS notice, etc.) to Arabic via a lookup dictionary. | MISSING | Searched src/ for the Arabic strings (`كشف حساب`, etc.) — no matches. Staff must type Arabic text manually (`messageArabic` free-text field); the auto-translation dictionary was not ported. |
| 8755 | `alTaxV5ClientArabicGreeting_` | Generates an Arabic greeting ("السادة/ ...") for a client. | MISSING | Same search as above — no Arabic greeting template found anywhere in src/. |
| 8760 | `alTaxV5CommunicationBodyForPreference_` | Combines English/Arabic message bodies based on the client's language preference. | FULL | src/modules/communications/communications.routes.ts `communicationBodyForPreference()` — doc comment: "English/Arabic selection logic is independently verifiable", same combining rules (English-only, Arabic-then-English, English-then-Arabic). |
| 8773 | `alTaxV5PortalAllDataTables_` | Lists every non-restricted schema table. | N/A-INTERNAL | Helper of the monolithic portal-data-blob system (see 8866), superseded by per-resource REST endpoints. |
| 8779 | `alTaxV5UniquePortalTables_` | Dedupes and filters a table-name list against the schema/restricted set. | N/A-INTERNAL | Same as above. |
| 8788 | `alTaxV5EmptyPortalData_` | Builds an empty `{table: []}` skeleton for all portal tables. | N/A-INTERNAL | Same as above. |
| 8796 | `alTaxV5PortalTablesForRole_` | Hardcoded list of which of ~20 tables each portal role (admin/employee/client/staff) may see. | PARTIAL | Role-based visibility is confirmed re-implemented per-resource in src/modules/{tasks,documents,communications,billing,accounting}.routes.ts (spot-checked); however, client/staff-role visibility for v3_Manual_JE, v3_GL_Entries, v3_Payment_Methods, v3_Check_Settings, v3_Contractor_Payments specifically was not individually confirmed — the single authoritative table list this function represents has no direct 1:1 counterpart. |
| 8857 | `alTaxV5ReadPortalTables_` | Bulk-reads a set of tables into one data object. | N/A-INTERNAL | Generic multi-table reader for the blob approach; superseded by individual REST GETs. |
| 8866 | `alTaxV3PortalFilterData_` | The master per-role data filter: for staff, filters ~20 tables down to records tied to the user's assigned clients/tasks; for client/employee roles, scopes to their own client/employee ID. | PARTIAL | Core scoping goal confirmed re-implemented via `canAccessClient`/`canAccessTask` (src/common/assignment.ts) across tasks, documents, communications, and billing routes (spot-checked, matches). Not exhaustively re-verified for every one of the ~20 tables the legacy function touches (e.g. GL entries, check settings) — see 8796 note. |
| 9023 | `alTaxV5EnsureQuarterlyMDWithholdingRule_` | Auto-seeds a hardcoded "TR-014Q" quarterly MD Withholding task rule as a side effect of nearly every task read. | MISSING | src/modules/rules/rules.routes.ts doc comment explicitly lists this as "Deliberately NOT ported ... if that rule is needed, create it explicitly via POST /rules like any other rule." |
| 9091 | `alTaxPortalLogin` | Password step of login; always creates a mandatory 2FA challenge and emails a 6-digit code before granting a session. | PARTIAL | src/modules/auth/auth.routes.ts POST `/login` exists and does the password/lockout check, but only issues a 2FA challenge `if (result.totpEnabled)` — for the many users who haven't opted into TOTP, login succeeds with password alone. Legacy enforced 2FA on every single login unconditionally. |
| 9123 | `alTaxPortalVerifyLoginCode` | Verifies the emailed 6-digit code and completes login. | PARTIAL | src/modules/auth/auth.routes.ts POST `/login/verify-totp` covers the "verify second factor" step but via authenticator-app TOTP, not an emailed one-time code, and only when the user has opted in — same mandatory-vs-optional gap as 9091. |
| 9143 | `alTaxPortalRefreshData` | Re-resolves the current user and re-fetches their filtered data blob. | FULL | Achieved architecturally by the frontend re-calling individual REST GET endpoints (tasks/documents/billing/etc.) rather than one refresh call — same end-user capability (get current data), different (REST-native) implementation. |
| 9157 | `alTaxPortalSetPassword` | Consumes an invite token + email to set an initial password (8-char minimum, single-use token). | FULL | src/modules/auth/auth.routes.ts POST `/accept-invite` — doc comment: "ported from alTaxPortalSetPassword ... Matches legacy exactly: email+token must both match the same row, token must not be expired, then password is set and the token is consumed". |
| 9197 | `alTaxPortalChangePassword` | Verifies current password and sets a new one (8-char minimum) for a signed-in user. | FULL | src/modules/auth/auth.routes.ts POST `/change-password`, same current-password verification and 8-char minimum. |
| 9233 | `alTaxV5FindUserByIdentifier_` | Finds a v3_Users row by UserID OR by email. | PARTIAL | src/modules/users/users.routes.ts admin routes (`/:userId/resend-invite`, `/reset-invite`, `/temporary-password`) look up strictly by `user_id` param — no email-fallback match like legacy's OR-email lookup. |
| 9245 | `alTaxPortalResendUserInvite` | Re-issues (or re-returns) an invite token/link, only minting a new one if expired/missing, and emails it. | PARTIAL | src/modules/users/users.routes.ts POST `/:userId/resend-invite` — doc comment: "ported from alTaxPortalResendUserInvite", same needsNewToken logic, but no email is sent (no email infra); link/token returned in response only. |
| 9283 | `alTaxPortalResetUserInvite` | Unconditionally wipes the password and issues a fresh invite token, and emails it. | PARTIAL | src/modules/users/users.routes.ts POST `/:userId/reset-invite` — doc comment: "ported from alTaxPortalResetUserInvite", same unconditional wipe+reissue, but again no email sent. |
| 9311 | `alTaxPortalSetUserTemporaryPassword` | Admin sets a one-time temp password for a user, shown once in the UI (never emailed in legacy either). | FULL | src/modules/users/users.routes.ts POST `/:userId/temporary-password` — same `ALTAX-` prefixed random temp password, returned in response, MustResetPassword forced true. |
| 9333 | `alTaxPortalAutoFixCenter` | Admin "repair everything" button: ensures tables/headers, seeds default tax rates + COA, seeds the MD Withholding rule, cleans duplicate imported sheet tabs. | PARTIAL | src/modules/system/system.routes.ts POST `/seed-defaults` covers the tax-rate + COA seeding (idempotent, matches by id/name). The table/header-repair and duplicate-tab-cleanup steps are N/A (real DB has migrations, no sheet tabs); the MD Withholding rule seed step is confirmed MISSING (see 9023). |
| 9348 | `alTaxV3ResolvePortalUser_` | Resolves the "current" portal user from an email/role pair (falling back to active Google session email). | FULL | Replaced by JWT-based `requireAuth` middleware (src/common/requireAuth.ts) which decodes the session token into `req.user` on every request — same purpose (identify the calling user), different (stateless) mechanism. |
| 9360 | `alTaxV3PortalClientAllowed_` | Checks whether a user has access to a given client (admin=all, client/employee=own, staff=derived from full portal-data-filter membership). | PARTIAL | src/common/assignment.ts `canAccessClient()` — doc comment itself states this is "a narrower, safe subset of that same rule": staff access is derived only from task assignments, not the full multi-table membership legacy computes (which also considers document requests, invoices, etc. tied to the client). |
| 9372 | `alTaxV3PortalTaskAllowed_` | Checks whether a user may act on a specific task (admin=all, client=never, staff/employee=assigned-or-client-access). | FULL | src/modules/tasks/tasks.routes.ts `canAccessTask()` — doc comment: "Mirrors alTaxV3PortalTaskAllowed_", identical admin/client/assigned/client-fallback branches. |
| 9381 | `alTaxV5IsInternalWorkItem_` | Detects whether a work item should be treated as an internal (non-client) firm task. | FULL | src/modules/tasks/tasks.routes.ts POST `/` doc comment: "Pass internalTask:true (or clientId 'C-ALTAX70') for firm-internal work"; `ensureInternalClient()` auto-creates the same `C-ALTAX70` placeholder client. |
| 9387 | `alTaxPortalCreateWorkItem` | Creates a task (staff/admin) or redirects client-role callers into document-request self-service creation; supports multi-client fan-out. | PARTIAL | Admin/staff task creation ported at src/modules/tasks/tasks.routes.ts POST `/` (doc comment: "ported from alTaxV3CreateManualTask_"). The client-role branch (client submits their own document request) has no equivalent — src/modules/documents/documents.routes.ts POST `/requests` requires `requireRole("admin","staff")` only. |
| 9453 | `alTaxPortalUpdateTaskStatus` | Updates just a task's Status field and notifies assigned staff. | PARTIAL | Status changes are covered generically by src/modules/tasks/tasks.routes.ts PATCH `/:taskId` (which accepts `status` among other allow-listed fields, with audit logging and auto-archive-on-completion), but the staff notification step (`alTaxV5NotifyStaffTask_`) has no equivalent call in the route — no notification/email infra exists. |
| 9478 | `alTaxPortalUpdateTask` | Edits an allow-listed set of task fields with per-field audit diff and staff notification. | PARTIAL | src/modules/tasks/tasks.routes.ts PATCH `/:taskId` — same allow-listed-field/audit-diff pattern (`TASK_UPDATABLE_FIELDS`), but again no staff-notification equivalent is fired on edit. |
| 9528 | `alTaxPortalVoidTask` | Soft-voids a task (Status=Void, timestamped note appended), blocked for client role. | FULL | src/modules/tasks/tasks.routes.ts POST `/:taskId/void` — doc comment: "ported from alTaxPortalVoidTask: soft status change", identical note-append pattern and access check. |
| 9550 | `alTaxPortalDeleteTask` | Admin-only hard row delete of a task (no confirm-text gate). | MISSING | src/modules/tasks/tasks.routes.ts module doc comment: "Deliberately NOT ported: alTaxPortalDeleteTask / alTaxPortalDeleteTasks: hard row delete, admin-only, with no confirm-text gate at all ... Void is the safe, reversible substitute." No DELETE route exists for `/tasks/:taskId`. |
| 9570 | `alTaxPortalDeleteTasks` | Bulk hard-delete of tasks by ID list. | MISSING | Same doc comment as above; POST `/bulk` in tasks.routes.ts only supports `action: "complete" \| "void"`, not delete. |
| 9603 | `alTaxPortalCompleteTasks` | Bulk-marks tasks Completed, with per-task access check and audit logging. | FULL | src/modules/tasks/tasks.routes.ts POST `/bulk` with `action: "complete"` — doc comment: "ported from alTaxPortalCompleteTasks", same per-task access check + audit log, plus adds auto-archive-on-completion. |

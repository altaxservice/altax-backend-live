# Parity Audit — Chunk 4 (Code.gs lines 9657–11657)

**Total functions audited: 65**

| Verdict | Count |
|---|---|
| FULL | 26 |
| PARTIAL | 8 |
| MISSING | 6 |
| N/A-INTERNAL | 18 |
| N/A-DEAD | 0 |
| N/A-SHEET-ONLY | 7 |

| Line | Function | Purpose | Verdict | Evidence |
|---|---|---|---|---|
| 9657 | `alTaxPortalDeleteDocumentRequest` | Hard-delete a single document request row (admin-only). | MISSING | checked src/modules/documents/documents.routes.ts — module doc comment explicitly lists this as "Deliberately NOT ported" (ungated hard delete); no route exists. |
| 9674 | `alTaxPortalDeleteDocumentRequests` | Bulk hard-delete document request rows. | MISSING | checked src/modules/documents/documents.routes.ts — same "deliberately not ported" note as above; no bulk-delete route exists. |
| 9705 | `alTaxPortalUpdateDocumentStatus` | Portal wrapper: resolves user, checks client/assigned access, restricts client role to a few statuses, then updates status. | FULL | src/modules/documents/documents.routes.ts `POST /requests/:requestId/status` — same access checks (canAccessDocumentRequest) and status update. |
| 9730 | `alTaxPortalUpdateDocumentRequest` | Edit allow-listed document request fields with per-field audit diff; blocks client role. | FULL | src/modules/documents/documents.routes.ts `PATCH /requests/:requestId` — same allow-list (REQUEST_UPDATABLE_FIELDS) and requireRole("admin","staff"). |
| 9769 | `alTaxPortalRemoveDocumentFile` | Remove/replace a file from a document request, falling back to next available upload. | FULL | src/modules/documents/documents.routes.ts `POST /uploads/:uploadId/remove` — mirrors fallback-to-next-upload logic. |
| 9835 | `alTaxPortalHideClientDocumentUploads` | Client self-hides uploaded files from their own portal view. | FULL | src/modules/documents/documents.routes.ts `POST /uploads/hide` — requireRole("client") only, sets hidden_from_client. |
| 9876 | `alTaxPortalUpdateClientProfile` | Portal wrapper: blocks client-role edits, checks access, delegates to core update fn. | FULL | src/modules/clients/clients.routes.ts `PATCH /:clientId` — requireRole("admin","staff") + canAccessClient. |
| 9895 | `alTaxV3NextClientId_` | Generates next sequential `C-####` client ID. | FULL | src/modules/clients/clients.routes.ts `nextClientId()` — identical regex-based max+1 logic. |
| 9909 | `alTaxV3ClientProfileExtraHeaders_` | List of extra Sheet column headers for client profile fields. | N/A-SHEET-ONLY | Postgres schema (sql/schema_source.json `v3_Clients`) defines these as fixed columns; no header-management concept applies. |
| 9927 | `alTaxV3EnsureHeaders_` | Generic helper: adds missing header columns to a Sheet if not present. | N/A-SHEET-ONLY | Sheet-column-mutation mechanics; schema migrations serve this purpose in the new app. |
| 9944 | `alTaxV3EnsureClientProfileHeaders_` | Calls EnsureHeaders_ with the client profile extra header list. | N/A-SHEET-ONLY | Same as above — depends on 9909/9927, both sheet-only. |
| 9948 | `alTaxV3CreateClientProfile_` | Creates a client row from a large field set; optionally also creates a portal user + invite token inline. | PARTIAL | src/modules/clients/clients.routes.ts `POST /` accepts only clientId/clientName/entityType/status/state/email/phone/assignedTo/portalEnabled at creation (the rest of the ~30 legacy fields require a follow-up `PATCH /:clientId`); the inline "createPortalUser" + invite-token issuance is not part of client creation at all — it requires a separate `POST /users` call. |
| 10047 | `alTaxPortalCreateClient` | Portal wrapper, legacy restricts to Admin only, delegates to CreateClientProfile_. | PARTIAL | src/modules/clients/clients.routes.ts `POST /` — same field-subset/no-inline-invite gap as 9948; also access was widened from admin-only to `requireRole("admin","staff")`. |
| 10058 | `alTaxPortalSendClientInvite` | Creates/updates the `usr_<clientId>` portal user for a client and issues/reuses a 7-day invite token, checks for duplicate email+role. | FULL | src/modules/users/users.routes.ts `POST /` — generic user upsert covers role="Client" + assignedClientId, same duplicate-email+role check and needsNewToken reuse logic (comment explicitly notes it covers alTaxPortalSaveStaffUser-equivalent behavior for any role). |
| 10124 | `alTaxV3FindInvoice_` | Helper: find invoice row by ID/row number, read as object. | N/A-INTERNAL | Supports invoice mutation routes; mirrored inline via `queryOne(... WHERE invoice_id = $1)` throughout src/modules/billing/billing.routes.ts. |
| 10132 | `alTaxPortalSendInvoice` | Emails invoice details to the client via MailApp. | MISSING | checked src/modules/billing/billing.routes.ts — module doc comment explicitly lists this as "Deliberately NOT ported ... no email infra exists here." |
| 10174 | `alTaxV5PortalInvoiceAllowed_` | Access-check helper: admin=yes, client=no, staff=canAccessClient. | N/A-INTERNAL | Mirrored by `canMutateInvoice()` in src/modules/billing/billing.routes.ts, same three-branch logic. |
| 10180 | `alTaxPortalUpdateInvoice` | Edit invoice fields, recompute status/balance from total/paid. | FULL | src/modules/billing/billing.routes.ts `PATCH /invoices/:invoiceId` — same total/paid/balance/status derivation. |
| 10212 | `alTaxPortalVoidInvoice` | Void an invoice (Status=Void, BalanceDue=0), blocks client role. | FULL | src/modules/billing/billing.routes.ts `POST /invoices/:invoiceId/void`. |
| 10232 | `alTaxV5BankLast4_` | Derive last-4 digits of an account number, or use explicit fallback. | N/A-INTERNAL | Equivalent inline digit-slicing logic in paymentMethods.routes.ts and billing.routes.ts payment recording. |
| 10237 | `alTaxV5OnlyDigits_` | Strip non-digit characters from a string. | N/A-INTERNAL | Mirrored by `onlyDigits()` in src/modules/paymentMethods/paymentMethods.routes.ts. |
| 10241 | `alTaxV5PaymentMethodRequiresBank_` | Returns true if method type (ach/check/direct deposit/wire) needs bank fields. | N/A-INTERNAL | Mirrored by `requiresBankDetails()` in src/modules/paymentMethods/paymentMethods.routes.ts, identical type list. |
| 10246 | `alTaxV5PaymentMethodById_` | Look up a payment method row by ID. | N/A-INTERNAL | Covered by `resolvePaymentMethod()` in src/common/accountingHelpers.ts. |
| 10256 | `alTaxV5DefaultPaymentMethodForUse_` | Find a client's default payment method for payroll or invoice use. | N/A-INTERNAL | Covered by `resolvePaymentMethod()` in src/common/accountingHelpers.ts (same UseForPayroll/UseForInvoices + Default* logic). |
| 10275 | `alTaxV5ResolvePaymentMethodForPayroll_` | Combine by-id lookup with default-for-payroll fallback. | N/A-INTERNAL | `resolvePaymentMethod(clientId, "payroll", id)` calls in src/modules/accounting/accounting.routes.ts payroll route. |
| 10279 | `alTaxV5EnforcePaymentMethodDefaults_` | When a payment method is marked default, clear the default flag on the client's other methods. | FULL | src/modules/paymentMethods/paymentMethods.routes.ts `POST /` — inline UPDATE clearing default_for_payroll/default_for_invoices on other rows for the client. |
| 10296 | `alTaxPortalCreateInvoice` | Create an invoice, blocks client/employee roles. | FULL | src/modules/billing/billing.routes.ts `POST /invoices`. |
| 10357 | `alTaxPortalDeleteInvoice` | Hard-delete invoice, confirm-text gated, blocked if payments exist. | MISSING | checked src/modules/billing/billing.routes.ts — module doc comment explicitly lists this as "Deliberately NOT ported... void is the safe, reversible substitute." |
| 10379 | `alTaxPortalRecordPayment` | Record a payment against an invoice, update AmountPaid/BalanceDue/Status. | FULL | src/modules/billing/billing.routes.ts `POST /invoices/:invoiceId/payments` — plus a documented intentional fix (caps AmountPaid at TotalAmount, which legacy's portal path did not do). |
| 10436 | `alTaxV5DateOnly_` | Normalize a value to a date-only (strip time). | N/A-INTERNAL | Mirrored by `dateOnly()` helper in src/modules/billing/billing.routes.ts. |
| 10443 | `alTaxV5DateString_` | Format a date as yyyy-MM-dd. | N/A-INTERNAL | Mirrored by `dateString()` helper in src/modules/billing/billing.routes.ts. |
| 10447 | `alTaxV5AddDays_` | Add N days to a date. | N/A-INTERNAL | Mirrored inline in the due-date calculation in `POST /recurring/run`. |
| 10453 | `alTaxV5NextRecurringDate_` | Compute next run date given a frequency (weekly/quarterly/annual/monthly). | N/A-INTERNAL | Mirrored inline in `POST /recurring/run` in src/modules/billing/billing.routes.ts, same frequency branches. |
| 10463 | `alTaxV5InvoiceSourceExists_` | Idempotency check: does an active invoice already exist for this SourceSystem/SourceRecordID? | N/A-INTERNAL | Mirrored inline query in `POST /recurring/run` (`source_system = 'Recurring Billing' AND source_record_id = $1 AND status <> 'void'`). |
| 10479 | `alTaxV5InvoicePaymentPayload_` | Resolve payment-method-derived fields (bank name, routing, account, last4) for an invoice payment. | N/A-INTERNAL | Covered by `resolvePaymentMethod(clientId, "invoices", ...)` in src/common/accountingHelpers.ts, used by the sales-receipt route. |
| 10497 | `alTaxPortalCreateSalesReceipt` | Create a paid invoice + matching payment in one step ("sales receipt"). | FULL | src/modules/billing/billing.routes.ts `POST /sales-receipt`. |
| 10602 | `alTaxPortalSaveRecurringBilling` | Create/update a recurring billing schedule; rejects AutoCollectPayment. | FULL | src/modules/billing/billing.routes.ts `POST /recurring` — same AutoCollectPayment rejection message. |
| 10658 | `alTaxPortalArchiveRecurringBilling` | Archive a recurring billing schedule (soft, Status=Archived). | FULL | src/modules/billing/billing.routes.ts `POST /recurring/:recurringBillingId/archive`. |
| 10675 | `alTaxPortalRunRecurringBilling` | Manually run due recurring schedules, creating invoices, idempotent per period. | FULL | src/modules/billing/billing.routes.ts `POST /recurring/run` — same skip/error/idempotency logic; AutoSendInvoice branch intentionally not ported (no email infra), documented in module comment. |
| 10777 | `alTaxPortalSaveTemplate` | Create/update a communication template, upsert by name if no ID. | FULL | src/modules/templates/templates.routes.ts `POST /`. |
| 10821 | `alTaxPortalSavePaymentMethod` | Create/update a payment method with account-confirmation and bank-field validation. | FULL | src/modules/paymentMethods/paymentMethods.routes.ts `POST /` — same validation rules, plus a documented security improvement (account/routing numbers encrypted at rest instead of legacy's plaintext storage). |
| 10880 | `alTaxPortalDeletePaymentMethod` | Hard-delete a payment method. | FULL | src/modules/paymentMethods/paymentMethods.routes.ts `POST /:clientId/:paymentMethodId/delete` — module comment explains this hard-delete (unlike client/task/invoice) was intentionally kept because no financial history depends on the row surviving. |
| 10897 | `alTaxPortalSaveCheckSettings` | Save per-client MICR/check-print calibration offsets. | FULL | src/modules/accounting/accounting.routes.ts `POST /check-settings` — same field set (CHECK_SETTING_NUMERIC_FIELDS matches all X/Y offsets). |
| 10954 | `alTaxV3FindRowById_` | Generic Sheet helper: find a row number by ID column value. | N/A-SHEET-ONLY | Sheet-row-lookup mechanics; replaced everywhere by `WHERE id_column = $1` SQL. |
| 10981 | `alTaxV3NextId_` | Generate an ID as prefix + timestamp + random 3-digit suffix. | N/A-INTERNAL | Mirrored by identical `idSuffix()`/`next*Id()` helpers duplicated across every routes.ts file. |
| 10986 | `alTaxV3ClientById_` | Look up a client object by ClientID. | N/A-INTERNAL | Mirrored via `queryOne(... WHERE client_id = $1)` throughout the codebase. |
| 10996 | `alTaxV5ClientByName_` | Look up a client by normalized name match. | N/A-INTERNAL | Supports 9948/11006; equivalent name-based duplicate check exists in src/modules/clients/clients.routes.ts `POST /` (`lower(client_name) = lower($1)`). |
| 11006 | `alTaxV5DocumentClientId_` | Reconcile a document request's ClientID against its (possibly stale/renamed) ClientName text field, preferring name match. | N/A-INTERNAL | Compensates for legacy's denormalized ClientName text drift in Sheets; not needed in the new schema, where document_requests.client_id is a proper foreign key to v3_clients — checked src/modules/documents/documents.routes.ts, no equivalent reconciliation logic exists or is needed. |
| 11022 | `alTaxV3WebOptions` | Returns centralized dropdown option lists (task types, immigration form types, request types, priorities, statuses, payment methods, communication channels, COA accounts, clients, staff) for web app forms. | MISSING | checked src/modules/system/system.routes.ts, src/modules/tasks/tasks.routes.ts, frontend/src/pages — no endpoint or hardcoded list matches legacy's option catalog (e.g. no "Sales Tax Filing"/"EFTPS Deposit"/immigration form list found anywhere in the new app). |
| 11126 | `alTaxV3CreateWorkItem` | Dispatcher: routes to create-task or create-document-request based on `mode`. | FULL | Functionally covered by two separate REST endpoints instead of one dispatcher: src/modules/tasks/tasks.routes.ts `POST /` (task mode) and src/modules/documents/documents.routes.ts `POST /requests` (document mode) — both branches individually ported. |
| 11148 | `alTaxV5InternalTaskClient_` | Ensure the internal placeholder client (AL TAX SERVICE / C-ALTAX70) exists, creating it if missing. | FULL | src/modules/tasks/tasks.routes.ts `ensureInternalClient()` — same client_id `C-ALTAX70` constant and ON CONFLICT DO NOTHING upsert. |
| 11181 | `alTaxV3CreateManualTask_` | Create a task row, auto-applying a matching Task Rule's defaults. | FULL | src/modules/tasks/tasks.routes.ts `POST /` — same matchingRule lookup and only-fill-if-not-supplied default logic. |
| 11223 | `alTaxV3CreateDocumentRequest_` | Create a document request row and trigger a notification. | PARTIAL | src/modules/documents/documents.routes.ts `POST /requests` creates the row (fields match), but does not call any equivalent of the notification step below — see 11262. |
| 11262 | `alTaxV5NotifyDocumentRequest_` | Auto-logs a bilingual (English/Arabic) Portal Note communication whenever a document request is created, notifying the other party (firm or client). | MISSING | checked src/modules/documents/documents.routes.ts and src/modules/communications/*.routes.ts — no communications-table insert or notification logic is triggered from document-request creation; grepped for "Notify", "CLIENT_DOCUMENT_REQUEST", "FIRM_DOCUMENT_REQUEST" with no hits. |
| 11298 | `alTaxV3UpdateClientProfile` | Core allow-listed client field update with per-field audit diff. | FULL | src/modules/clients/clients.routes.ts `PATCH /:clientId` — UPDATABLE_FIELDS list is a 1:1 match to the legacy `allowed` array (comment confirms "ported 1:1"). |
| 11368 | `alTaxV3UpdateDocumentStatus` | Core status-update logic: sets Status, stamps ReceivedDate on Received/Completed/Closed. | FULL | src/modules/documents/documents.routes.ts `POST /requests/:requestId/status` — same ReceivedDate auto-stamp logic. |
| 11392 | `updateUnifiedTaskStatus` | Update a task's status; auto-stamps FiledDate when status becomes "Completed" (using existing FiledDate if present). | PARTIAL | src/modules/tasks/tasks.routes.ts `PATCH /:taskId` handles status changes generically (and adds an auto-archive-on-completion feature legacy didn't have at this line), but does not auto-stamp `filed_date` when status flips to Completed — filedDate is only set if the caller explicitly includes it in the request body. |
| 11418 | `alTaxV3GlobalSearch` | Cross-table search across Clients, Tasks, Document_Requests, Invoices, and Payments (substring match across all fields). | PARTIAL | src/modules/search/search.routes.ts `GET /` — covers clients, tasks, invoices, and documents, but does not search the Payments table at all (legacy's 5th `specs` entry, `v3_Payments`, has no counterpart); field matching is also narrower (specific columns via ILIKE vs. legacy's join-all-fields substring match). |
| 11470 | `alTaxV5SetupProfessionalTables` | One-time admin action: create/repair all v5 Sheets tabs, seed default rates/COA, show UI alert. | N/A-SHEET-ONLY | Calls alTaxV3SetupSheets() (sheet-tab creation) — no meaning in a fixed Postgres schema; the seeding portions it delegates to are separately covered (see 11545/11614 verdicts) via src/modules/system/system.routes.ts `POST /seed-defaults`. |
| 11477 | `alTaxV5CleanDuplicateImportedTabs` | Finds/merges duplicate imported Sheet tabs (e.g. "v3_Clients (1)") into the canonical tab, hides duplicates. | N/A-SHEET-ONLY | Pure Google-Sheets-tab-management mechanics; no equivalent concept in a single Postgres table per entity. |
| 11529 | `alTaxV5EnsureTaxRateScopeColumns_` | Ensures the v3_Tax_Rates sheet has the required Scope/ClientID/etc columns, adding any missing ones. | N/A-SHEET-ONLY | sql/schema_source.json `v3_Tax_Rates` already defines all these columns (Scope, ClientID, ClientName, RateType, Rate, EmployeeEmployer, WageCap, State, Active, Notes) as fixed table columns. |
| 11545 | `alTaxV5SeedDefaultRates_` | Seeds ~12 default global tax rate rows (ST6, ST12, VAPE20, RATE60, SS_EE/ER, MED_EE/ER, FIT, STATE, FUTA, SUTA) if the table is empty, including WageCap for SS (184500) and FUTA (7000). | PARTIAL | src/modules/system/system.routes.ts `POST /seed-defaults` (DEFAULT_TAX_RATES) seeds the same rate IDs/values, but omits WageCap entirely for SS_EE/SS_ER/FUTA/SUTA — none of the seeded rows set a wage_cap value, unlike legacy's 184500/7000 caps. |
| 11569 | `alTaxV5DefaultCOARows_` | Data table of ~40 default chart-of-accounts rows spanning Asset/Liability/Equity/Income/COGS/Expense categories. | PARTIAL | Underlies 11614; new app's equivalent data table (DEFAULT_COA_ACCOUNTS in src/modules/system/system.routes.ts) has only 13 accounts, missing most of legacy's set (Prepaid Expenses, Fixed Assets, Accumulated Depreciation, Credit Card Payable, Owner Equity/Draw, Other Income, Rent, Utilities, Telephone/Internet, Insurance, Professional Fees, Bank/Merchant Fees, Advertising, Supplies, Meals, Travel, Auto, Repairs, Dues, Licenses, Postage, Depreciation Expense, Ask My Accountant). |
| 11614 | `alTaxV5SeedDefaultCOA_` | Seeds default COA rows (skipping ones that already exist by ID or name) if the table is empty. | PARTIAL | src/modules/system/system.routes.ts `POST /seed-defaults` seeds only the 13-account DEFAULT_COA_ACCOUNTS list (matches by name, not id, per its own comment) vs. legacy's ~40-account default set — same seed-if-missing behavior, much smaller default catalog. |
| 11637 | `alTaxV5Rate_` | Configurable rate lookup by RateID, preferring a client-specific override over the global rate, falling back to a caller-supplied default. | FULL | src/common/accountingHelpers.ts `lookupRate()` — line-for-line equivalent scope/client-override logic, used throughout src/modules/accounting/accounting.routes.ts. |

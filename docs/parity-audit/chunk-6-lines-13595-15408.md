# Parity Audit — Chunk 6 (Code.gs lines 13595–15408, end of file)

**Total functions audited: 73** — FULL: 5, PARTIAL: 7, MISSING: 21, N/A-INTERNAL: 31, N/A-SHEET-ONLY: 9, N/A-DEAD: 0

This chunk covers: contractor payment edit, employee archive/hard-delete, document upload (link + base64), task-attachment removal, an internal QA/seed test harness, the staff/portal notification and email-authorization plumbing, the Reminder Automation + Daily Operations Alert engine (Apps Script triggers), task/staff/client communications logging, and the Secure Vault (client secrets).

| Line | Function | Purpose | Verdict | Evidence |
|---|---|---|---|---|
| 13595 | `alTaxV5RepostContractorPaymentGl_` | Deletes and re-posts the 2 GL lines (expense debit / cash credit) for an edited contractor payment. | N/A-INTERNAL | Supports `alTaxPortalUpdateContractorPayment`; logic ported inline in `src/modules/accounting/accounting.routes.ts` PATCH `/contractor-payments/:contractorPaymentId` (lines 772–780, delete+re-insert GL rows). |
| 13622 | `alTaxPortalUpdateContractorPayment` | Edits a contractor payment (date, amount, method, bank details, 1099 flag, status) and reposts GL. | PARTIAL | `src/modules/accounting/accounting.routes.ts` PATCH `/contractor-payments/:contractorPaymentId` (lines 749–786) exists but only updates `payment_date, amount, expense_category, memo, method, check_number`. Missing: `PaymentMethodID`/bank name/routing/account/type/last4, `ConfirmationNumber`, `1099Eligible`, `Status`, and reassigning the contractor. |
| 13681 | `alTaxPortalDeleteEmployee` | Soft-archives an employee/contractor (sets Status=Archived). | FULL | `src/modules/accounting/accounting.routes.ts` POST `/employees/:employeeId/archive` (lines 933–951) — same soft-delete behavior, access-checked via `canAccessClient`. |
| 13697 | `alTaxPortalHardDeleteEmployee` | Permanently deletes an employee/worker row. | MISSING | Checked `accounting.routes.ts` — only the `/archive` route exists; module comment there explicitly documents soft-delete-only (hard delete would orphan paycheck/contractor-payment history). No hard-delete route anywhere. |
| 13713 | `alTaxPortalSaveDocumentUpload` | Uploads/links a document to a request or task, base64-to-Drive or link, marks request Received, can replace a task's prior attachment, sends a notification. | PARTIAL | `src/modules/documents/documents.routes.ts` POST `/uploads` (lines 258–321) ports the link-only path and request/task targeting + mark-Received logic. Module doc comment explicitly states the base64/DriveApp branch is "Deliberately NOT ported" (no Drive integration). Also drops the `replaceTaskAttachment` retire-old-uploads behavior and the upload notification email. |
| 13815 | `alTaxV5NotifyDocumentUpload_` | Sends an email/portal notice when a document is uploaded (client→firm or firm→client). | MISSING | Checked `documents.routes.ts` POST `/uploads` — only `logAudit` is called, no notification of any kind is sent; no email infra exists in this backend (confirmed no nodemailer/smtp/sendgrid usage anywhere in `src/`). |
| 13845 | `alTaxV5RetireTaskDocumentUploads_` | Marks all-but-one of a task's document uploads as Replaced/Removed. | N/A-INTERNAL | Supports the missing `alTaxPortalRemoveTaskAttachment` and the missing `replaceTaskAttachment` upload flow; no bulk-retire-by-taskId logic exists in `documents.routes.ts`. |
| 13863 | `alTaxPortalRemoveTaskAttachment` | Removes/retires all document uploads attached to a task. | MISSING | Checked `documents.routes.ts` and `src/modules/tasks/tasks.routes.ts` — only single-upload removal by `uploadId` exists (`POST /uploads/:uploadId/remove`, line 382); no taskId-scoped bulk-removal endpoint. |
| 13881 | `alTaxV5FindClientByEmailOrName_` | Looks up a client row by email or exact name match. | N/A-INTERNAL | Supports the QA test harness `alTaxPortalRunAccountingTestCase` only. |
| 13892 | `alTaxPortalRunAccountingTestCase` | Seeds a fixed test client/employee and runs a sample sales input, payroll input, and manual JE as an end-to-end QA smoke test. | MISSING | Checked `accounting.routes.ts` and ran `grep -rl "AccountingTest\|TestCase" src/modules/` — no seed/smoke-test endpoint exists in the new app. |
| 14049 | `alTaxV5FindPortalUserByAlias_` | Looks up an active portal user by email, name, or UserID. | N/A-INTERNAL | Generic user-lookup helper used by several other legacy functions outside this chunk's scope. |
| 14062 | `alTaxV5IsAssignableStaffRole_` | Checks whether a role is admin/staff/manager/owner (i.e., assignable staff). | N/A-INTERNAL | Role-check helper; new app's simplified role set (`admin\|staff\|client\|employee\|general`, see `src/modules/auth/auth.service.ts`) uses inline `requireRole("admin","staff")` checks instead. |
| 14066 | `alTaxV5AssignableStaffRows_` | Returns all active admin/staff/manager/owner user rows with valid emails. | N/A-INTERNAL | Roughly ported as the staff-directory query in `communications.routes.ts` GET `/staff-directory` (lines 246–251). |
| 14075 | `alTaxV5FindAssignableStaffByAlias_` | Resolves a staff alias (email/name/UserID) to an active staff/admin user row. | N/A-INTERNAL | Ported inline as the recipient lookup query in `communications.routes.ts` POST `/staff` (lines 262–269). |
| 14087 | `alTaxV5TaskNoticeBody_` | Builds the plain-text body for a staff task-update email notice. | N/A-INTERNAL | Supports the missing `alTaxV5NotifyStaffTask_` email feature; no equivalent body-builder needed since no such email is sent. |
| 14108 | `alTaxV5NotifyStaffTask_` | Emails the assigned staff member a notice when their task changes. | MISSING | Checked `tasks.routes.ts` and `communications.routes.ts` — no automatic email is sent when a task is updated/assigned; only manual task notes/messages exist (`POST /task`). |
| 14121 | `alTaxV5NotifyStaffTaskBatch_` | Batch version of the above — one email per staff member listing all their new tasks. | MISSING | Same as above; no batch task-assignment email exists in the new app. |
| 14151 | `alTaxV5CommunicationExistsToday_` | Prevents duplicate reminder communications from being logged twice on the same day. | MISSING | `communications.routes.ts` module doc comment explicitly lists this as "Deliberately NOT ported" ("a dedup nicety on top of the core log-write, not required for correctness"). |
| 14171 | `alTaxV5FirmInboxEmail_` | Resolves the firm's default admin inbox email for inbound notices. | N/A-INTERNAL | Only meaningful for the (missing) email-sending paths; not needed elsewhere in the new app. |
| 14184 | `alTaxV5IsValidEmail_` | Simple regex email-format validator. | N/A-INTERNAL | Trivial validation helper used throughout legacy; equivalent ad hoc validation exists in various new-app routes. |
| 14188 | `alTaxV5EmailAuthorizationMessage_` | Detects Apps Script MailApp authorization/quota errors and appends a fix-it hint. | N/A-SHEET-ONLY | Specific to Google Apps Script's `MailApp`/`script.send_mail` authorization model; meaningless without that runtime (no MailApp equivalent exists in the new app). |
| 14196 | `alTaxV5MenuAuthorizeEmailSending` | Spreadsheet menu item that checks/prompts for MailApp email authorization. | N/A-SHEET-ONLY | Spreadsheet custom-menu UI wiring (`SpreadsheetApp.getUi()`), no meaning in a web app. |
| 14206 | `alTaxPortalCheckEmailAuthorization` | Portal-callable check of remaining MailApp daily send quota, for staff to self-diagnose email issues. | MISSING | No email-sending infrastructure exists in the new backend (confirmed via repo-wide search for smtp/sendgrid/nodemailer — no hits besides comment references), so there is nothing to check quota on. |
| 14217 | `alTaxV5SendPortalNotice_` | Sends a one-off email notice to a portal user via MailApp and logs the attempt. | MISSING | Checked `communications.routes.ts` — the ported `POST /` and `POST /task` routes only write log rows (`Status='Saved'`); no email is ever actually sent (module doc comment: "No MailApp/SMS gateway sending anywhere"). |
| 14234 | `alTaxV5PortalNoticeBody_` | Builds a bilingual (English/Arabic) email body for portal notices, with a special case for document-request notices. | N/A-INTERNAL | Supports the missing `alTaxV5SendPortalNotice_`/`alTaxV5AppendCommunication_` sending path; the English/Arabic *preview* logic itself is ported (`communicationBodyForPreference` in `communications.routes.ts` lines 57–68), but this specific notice-email body format is not. |
| 14275 | `alTaxV5AppendCommunication_` | Core function that writes a communication row and (if `sendNow`) actually sends it via email or SMS gateway, with duplicate-guard and bilingual body selection. | PARTIAL | Record-write path ported to `communications.routes.ts` POST `/` (lines 89–127) and `communicationBodyForPreference` (preview only). Explicitly not ported: actual email/SMS sending (`sendNow`), duplicate-today guard, and the varying `Status` values (`Queued`/`Sent`/`Saved - Manual Send`/`Email Failed...`) — new app always writes `Status='Saved'`. |
| 14358 | `alTaxPortalCreateAutoReminders` | Scans open document requests, unpaid tax payments, overdue tasks, and paychecks to auto-generate reminder communications for clients/staff/employees. | MISSING | `communications.routes.ts` module doc comment explicitly lists this as "Deliberately NOT ported" (same category as the Task Rules and Recurring Billing engines deferred elsewhere). No equivalent route/cron job exists. |
| 14481 | `alTaxV5ReminderAutomationProps_` | Returns the PropertiesService store used to persist reminder-automation settings. | N/A-INTERNAL | Supports the missing Reminder Automation feature; PropertiesService has no Node equivalent invoked here. |
| 14485 | `alTaxV5ClearReminderAutomationProps_` | Clears the stored reminder-automation settings. | N/A-INTERNAL | Same as above. |
| 14500 | `alTaxV5DeleteReminderTriggers_` | Removes any existing `alTaxV5RunDailyReminderAutomation` time-based trigger. | N/A-INTERNAL | GAS `ScriptApp` trigger-management helper; no scheduler/cron subsystem exists in the new backend (`find . -iname "*cron*" -o -iname "*schedul*"` under `src/` returns nothing). |
| 14508 | `alTaxV5InstallReminderAutomation_` | Persists reminder settings and installs a daily time-based trigger at a chosen hour. | N/A-INTERNAL | Supports the missing `alTaxPortalSaveReminderAutomation`/menu install flows. |
| 14529 | `alTaxPortalSaveReminderAutomation` | Portal action: turns on daily automatic reminders at a configured hour/audience/channel. | MISSING | No scheduled-job/cron infrastructure exists anywhere in `src/` of the new backend; no equivalent settings route found. |
| 14540 | `alTaxPortalDisableReminderAutomation` | Portal action: turns off daily automatic reminders. | MISSING | Same — no automation exists to disable. |
| 14552 | `alTaxV5RunDailyReminderAutomation` | The actual trigger-fired job body that runs `alTaxPortalCreateAutoReminders` on schedule. | MISSING | No scheduled job runner exists in the new backend. |
| 14566 | `alTaxV5MenuInstallDailyReminderTrigger` | Spreadsheet menu prompt to install the daily reminder trigger. | N/A-SHEET-ONLY | Spreadsheet custom-menu UI (`SpreadsheetApp.getUi().prompt`). |
| 14589 | `alTaxV5MenuRunDailyReminderAutomation` | Spreadsheet menu item to run the reminder automation once, on demand. | N/A-SHEET-ONLY | Spreadsheet menu wiring. |
| 14599 | `alTaxV5MenuRemoveDailyReminderTrigger` | Spreadsheet menu item to remove the reminder trigger. | N/A-SHEET-ONLY | Spreadsheet menu wiring. |
| 14610 | `alTaxV5OpenStatusKey_` | Normalizes a task status string to lowercase/trimmed. | N/A-INTERNAL | Trivial normalization helper for the Daily Operations Alert engine. |
| 14614 | `alTaxV5IsOpenTask_` | Predicate: task status is not completed/closed/archived/void. | N/A-INTERNAL | Supports the missing Daily Operations Alert engine's task filtering. |
| 14618 | `alTaxV5TaskDueDate_` | Resolves a task's effective due date from Agency/Staff/Due date fields. | N/A-INTERNAL | Same. |
| 14624 | `alTaxV5StartOfDay_` | Zeroes out the time portion of a Date. | N/A-INTERNAL | Same. |
| 14628 | `alTaxV5TaskRequirement_` | Derives a human-readable "what's required to close this task" string from its service line/name. | N/A-INTERNAL | Supports the Daily Operations Alert email body only. |
| 14639 | `alTaxV5DailyTaskLine_` | Formats one task as a single summary line for alert emails. | N/A-INTERNAL | Same. |
| 14650 | `alTaxV5StaffTaskBlock_` | Formats one task as a multi-line block for per-staff alert emails. | N/A-INTERNAL | Same. |
| 14662 | `alTaxV5BuildDailyOperationsBody_` | Assembles the full firm-wide Daily Operations Alert email body (system health, workload, overdue/due-soon/waiting tasks). | N/A-INTERNAL | Supports the missing `alTaxV5SendDailyOperationsAlert_`. |
| 14716 | `alTaxV5SendStaffAssignedTaskEmails_` | Emails each staff member their own overdue/due-soon/waiting task alert (and optional SMS gateway alert). | MISSING | No per-staff daily digest email exists anywhere in the new backend; confirmed no scheduler and no email infra. |
| 14786 | `alTaxV5SendDailyOperationsAlert_` | Builds and sends the firm-wide Daily Operations Alert email plus triggers staff emails. | MISSING | Same — no equivalent engine or route. |
| 14829 | `alTaxPortalSendDailyOperationsAlert` | Portal action: admin manually triggers the Daily Operations Alert immediately. | MISSING | No route/endpoint found for a manual "send daily alert now" action in `accounting`/`communications`/`system` modules. |
| 14838 | `alTaxV5DailyOperationsAlertProps_` | Returns the PropertiesService store for Daily Ops Alert settings. | N/A-INTERNAL | Supports the missing feature. |
| 14842 | `alTaxV5DeleteDailyOperationsAlertTriggers_` | Removes existing Daily Ops Alert triggers. | N/A-INTERNAL | GAS trigger cleanup; no scheduler exists to clean up in the new app. |
| 14850 | `alTaxV5InstallDailyOperationsAlert_` | Persists settings and installs the daily Ops Alert trigger at a chosen hour. | N/A-INTERNAL | Supports the missing feature. |
| 14864 | `alTaxV5RunDailyOperationsAlertTrigger` | Trigger-fired job body that runs the Daily Operations Alert on schedule. | MISSING | No scheduled job runner exists in the new backend. |
| 14870 | `alTaxPortalInstallDailyOperationsAlert` | Portal action: admin schedules the daily firm-wide alert at a chosen hour. | MISSING | No settings route for this exists. |
| 14881 | `alTaxPortalDisableDailyOperationsAlert` | Portal action: admin disables the daily firm-wide alert schedule. | MISSING | No settings route for this exists. |
| 14893 | `alTaxV5MenuRunDailyOperationsAlert` | Spreadsheet menu item to send the Daily Operations Alert on demand. | N/A-SHEET-ONLY | Spreadsheet menu wiring. |
| 14903 | `alTaxV5MenuInstallDailyOperationsAlertTrigger` | Spreadsheet menu prompt to install the Daily Ops Alert trigger. | N/A-SHEET-ONLY | Spreadsheet menu wiring. |
| 14920 | `alTaxV5MenuRemoveDailyOperationsAlertTrigger` | Spreadsheet menu item to remove the Daily Ops Alert trigger. | N/A-SHEET-ONLY | Spreadsheet menu wiring. |
| 14932 | `alTaxV5DefaultTaskMessageRecipient_` | Auto-resolves a default recipient (assigned staff, else any admin/owner/manager, else any staff) when a task message has none specified. | MISSING | `communications.routes.ts` POST `/task` (lines 215–216) requires an explicit `recipientEmail`/`sentTo`/`recipient` and returns 400 if missing — no fallback-resolution logic exists. |
| 14956 | `alTaxV5TaskCommunicationEmailBody_` | Builds the email body for a sent task message. | N/A-INTERNAL | Supports the (unported) `sendNow` email-delivery branch of task communications. |
| 14977 | `alTaxPortalCreateTaskCommunication` | Saves a task note or message, resolves a default recipient, optionally sends it now by email. | PARTIAL | Ported at `communications.routes.ts` POST `/task` (lines 199–238) — note/message save and access checks match, but default-recipient auto-resolution (see 14932) and actual `sendNow` email delivery are both dropped; `Status` is always `'Saved'`. |
| 15054 | `alTaxPortalCreateCommunication` | Client-facing entry point to log/send a communication (email/SMS/portal note) to a client. | PARTIAL | Ported at `communications.routes.ts` POST `/` (lines 89–127) — record-write and access-check (`canAccessClient`) match; actual sending (email/SMS/portal-notice) and the duplicate-today guard are dropped (see 14275/14151). |
| 15078 | `alTaxPortalCreateStaffCommunication` | Sends/logs a staff-to-staff message via email, SMS gateway, WhatsApp, or phone, with per-channel status tracking. | PARTIAL | Ported at `communications.routes.ts` POST `/staff` (lines 257–288) — recipient resolution and row logging match; channel-specific behavior (SMS gateway send, WhatsApp/phone "Saved - Manual Send" states, actual email send) is collapsed to always `Status='Saved'`, no gateway/email dispatch performed. |
| 15154 | `alTaxV5EnsureVaultTables_` | Ensures the `v3_Client_Secrets` and `v3_Secret_Access_Log` sheets exist. | N/A-SHEET-ONLY | Dynamic sheet/table provisioning is a Google-Sheets-backend mechanic; the new app's Postgres tables (`altax.v3_client_secrets`, `altax.v3_secret_access_log`) already exist via schema migration, so there's nothing to "ensure" at request time. |
| 15159 | `alTaxV5VaultUser_` | Requires an authenticated portal user with the Admin role (stricter than the general admin-only flag) to open the vault. | N/A-INTERNAL | Ported as `vaultRouter.use(requireAuth, requireRole("admin"))` in `src/modules/vault/vault.routes.ts` (line 50). |
| 15165 | `alTaxV5VaultClient_` | Looks up a client by ID for vault operations, throwing if not found. | N/A-INTERNAL | Ported inline as the `queryOne(...v3_clients...)` + 404 check repeated in each `vault.routes.ts` route. |
| 15171 | `alTaxV5VaultAudit_` | Writes a row to the Secret Access Log for a successful vault action. | N/A-INTERNAL | Ported as the `vaultAudit()` helper in `vault.routes.ts` (lines 37–47), called on every successful list/save/reveal/delete/reset. |
| 15191 | `alTaxV5VaultDenied_` | Writes a Secret-Access-Log row AND a general audit-log row whenever vault access is denied (wrong role, client not found, etc.), used from every vault function's catch block. | PARTIAL | Some 404 paths in `vault.routes.ts` call `vaultAudit(...)` inline (list at line 60, save at line 86), but role-based denials go through `requireRole("admin")` middleware (`src/common/requireAuth.ts` lines 26–32), which returns 403 with **no audit-log write at all**. The `reset` route's client-not-found 404 (line 203) also writes no audit row. So unauthorized-access attempts are blocked but no longer comprehensively logged, unlike legacy's defense-in-depth logging of every denial. |
| 15218 | `alTaxV5SafeVaultRow_` | Whitelists which vault-row fields are safe to return to the client (excludes raw payload internals like Salt/IV from bulk responses — though it does still include `EncryptedPayload`). | N/A-INTERNAL | Ported implicitly via the explicit column `SELECT` list in `vault.routes.ts` GET `/:clientId` (lines 64–70), which is actually stricter than legacy (excludes the encrypted payload entirely from the list response; only the dedicated `/reveal` route returns decrypted content). |
| 15230 | `alTaxPortalListClientSecrets` | Lists a client's active (non-deleted) vault secrets, metadata only. | FULL | `vault.routes.ts` GET `/:clientId` (lines 56–75). |
| 15247 | `alTaxPortalSaveClientSecret` | Creates/updates a vault secret; legacy expects the browser to send pre-encrypted payload/salt/IV. | FULL | `vault.routes.ts` POST `/:clientId` (lines 82–136) — functionally equivalent (create/update a secret with category/label required validation), but the encryption model is deliberately changed: server now encrypts a plaintext `secret` field itself (`encryptValue`) rather than trusting client-supplied ciphertext, per the module's documented Option-B redesign (v3_Client_Secrets had zero production rows, so no migration was needed). |
| 15311 | `alTaxPortalDeleteClientSecret` | Soft-deletes one vault secret (clears payload, sets Status=Deleted). | FULL | `vault.routes.ts` POST `/:clientId/:secretId/delete` (lines 172–190). |
| 15349 | `alTaxPortalResetClientVault` | Resets (soft-deletes) every active secret for a client, gated by typing "RESET". | FULL | `vault.routes.ts` POST `/:clientId/reset` (lines 197–221) — same confirmation gate and bulk soft-delete behavior. |
| 15387 | `alTaxPortalLogSecretAccess` | Generic client-callable endpoint to log an arbitrary vault-access action/field/result (e.g., a frontend "copied value" event) to the Secret Access Log. | MISSING | Checked `vault.routes.ts` in full — only route-specific auto-logging exists for OPEN/CREATE/UPDATE/REVEAL/DELETE/RESET; there is no standalone endpoint accepting an arbitrary client-supplied action to audit (e.g., a "user copied secret to clipboard" event triggered purely from the frontend). |


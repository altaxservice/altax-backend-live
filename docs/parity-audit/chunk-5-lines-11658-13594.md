# Parity Audit — Chunk 5 (Code.gs lines 11658–13594)

**Total functions audited: 52**
Verdict counts: FULL = 19, PARTIAL = 9, MISSING = 6, N/A-INTERNAL = 15, N/A-SHEET-ONLY = 3, N/A-DEAD = 0 (sum = 52)

| Line | Function | Purpose | Verdict | Evidence |
|---|---|---|---|---|
| 11658 | `alTaxV5Money_` | Coerces a value to a number, defaulting to 0. | N/A-INTERNAL | Ported as `money()` in `src/common/accountingHelpers.ts` (used throughout accounting.routes.ts). |
| 11663 | `alTaxV5AppendGl_` | Appends one GL entry row for a client with defaulted fields. | N/A-INTERNAL | Ported as `appendGl()` in `src/common/accountingHelpers.ts`. |
| 11683 | `alTaxV5RequirePortalUser_` | Resolves the calling portal user by email, optionally requiring admin role. | N/A-INTERNAL | Equivalent access-control now done via `requireAuth`/`requireRole` middleware in `src/common/requireAuth.ts`, used on every route below. |
| 11690 | `alTaxV5RequireFirmUser_` | Requires the caller to be AL TAX staff (not client/employee role). | N/A-INTERNAL | Equivalent via `requireRole("admin","staff")` guard used on accounting/employee routes in `src/modules/accounting/accounting.routes.ts`. |
| 11699 | `alTaxPortalSaveSalesInput` | Records a sale, computes total sales tax due from configurable rates, posts 3 GL lines. | FULL | `src/modules/accounting/accounting.routes.ts` POST `/sales` (lines 218-272) — same rate lookups (ST6/ST12/VAPE20/RATE60) and same 3 GL lines. |
| 11779 | `alTaxV5FindEmployeeForPaycheck_` | Finds an employee row by client+name for payroll processing. | N/A-INTERNAL | Inlined as SQL lookup in `accounting.routes.ts` POST `/payroll` (lines 365-368). |
| 11791 | `alTaxV5IsContractorWorker_` | Determines from worker/pay/form-type fields whether a worker row is a contractor. | N/A-INTERNAL | Inlined as `workerSignal`/`contractorSignal` string checks in `accounting.routes.ts` (lines 374-376, 695-697). |
| 11804 | `alTaxV5PayrollField_` | Reads a data field trying a list of alternate key names, with fallback. | N/A-INTERNAL | Inlined via `body.x ?? body.y` / `!== undefined` patterns throughout `accounting.routes.ts` payroll routes. |
| 11814 | `alTaxV5PayrollMoney_` | Money-coerced version of `alTaxV5PayrollField_`. | N/A-INTERNAL | Same as above, combined with `money()`. |
| 11818 | `alTaxV5PayrollCalcFields_` | Copies computed paycheck fields onto a target row object. | N/A-INTERNAL | Inlined directly into INSERT/UPDATE parameter lists in `accounting.routes.ts` (e.g. lines 442-471). |
| 11847 | `alTaxV5CalculatePaycheck_` | Full flat-rate paycheck calculation: gross, OT, deductions, taxable wages, employee/employer taxes, net pay. | FULL | `accounting.routes.ts` POST `/payroll` (lines 379-429) — verified formula-by-formula match, including the flat-rate (non-bracket) approach and override behavior for federal/state amounts. |
| 11945 | `alTaxPortalSavePayrollInput` | Saves a payroll input record + linked paycheck, posts payroll GL entries. | PARTIAL | `accounting.routes.ts` POST `/payroll` (lines 352-490) matches validation and calculation, but GL posting is reduced: legacy posts 5 lines (wages debit; net-pay cash credit; deduction-payable credit when deductions exist; employer-tax expense debit; payroll-tax-payable credit for employee+employer taxes). New app posts only 3 lines (wages debit, employer-tax-expense debit, single cash credit for netPay+employeeTaxes+employerTaxes combined) — no separate "Payroll Deduction Payable" or "Payroll Tax Payable" liability accounts are ever posted. |
| 12121 | `alTaxV5DeleteRowsWhere_` | Deletes all sheet rows matching a predicate. | N/A-INTERNAL | Equivalent to plain `DELETE FROM ... WHERE ...` SQL statements used throughout `accounting.routes.ts` (e.g. line 325, 585, 772). |
| 12132 | `alTaxV5RepostSalesGl_` | Deletes and reposts the 3 GL lines for a sale (used on edit). | FULL | `accounting.routes.ts` PATCH `/sales/:saleId` (lines 325-337). |
| 12169 | `alTaxPortalUpdateSalesInput` | Edits a sales input record, recalculates tax, reposts GL. | FULL | `accounting.routes.ts` PATCH `/sales/:saleId` (lines 290-343). |
| 12214 | `alTaxV5PaycheckLockedForEdit_` | True if a paycheck's status/PrintedAt means it's locked from editing. | PARTIAL | `accounting.routes.ts` PATCH `/paychecks/:paycheckId` (line 518-520) only blocks edits when `status === 'void'`. Legacy locks on a much broader set (printed, finalized, void, reversed, deleted, paid, issued, processed, or any PrintedAt timestamp) — so in the new app a "Printed" paycheck can still be freely edited, unlike legacy. |
| 12221 | `alTaxV5PaycheckLockedForDelete_` | Same lock check but exempts test records, used to gate paycheck deletion. | MISSING | No paycheck delete route exists anywhere in `accounting.routes.ts` (grepped for "delete.*paycheck" — no match); the delete feature this helper gated was not ported at all (see `alTaxPortalDeleteAccountingRecord` below), so the lock check has no surface to apply to. |
| 12225 | `alTaxPortalMarkPaycheckPrinted` | Marks a paycheck as Printed with a timestamp. | FULL | Folded into `accounting.routes.ts` GET `/paychecks/:paycheckId/print` (lines 666-669) — the print action itself marks the check printed, per explicit module comment. |
| 12253 | `alTaxV5RepostPayrollGl_` | Deletes and reposts payroll GL entries (used on edit). | PARTIAL | `accounting.routes.ts` PATCH `/paychecks/:paycheckId` (lines 585-597) — same 3-line-vs-5-line GL reduction as `alTaxPortalSavePayrollInput` above (no deduction-payable / payroll-tax-payable liability lines posted). |
| 12312 | `alTaxPortalUpdatePayrollInput` | Edits a payroll input record AND its linked paycheck together, reposts GL. | PARTIAL | `accounting.routes.ts` PATCH `/paychecks/:paycheckId` (lines 511-603) edits the paycheck and reposts GL, but per its own doc comment does NOT update the twin `v3_payroll_input` row at all ("nothing in this API links the two records... payroll_input has no GET/detail route of its own"). Legacy edits both linked records together. Also inherits the looser lock check (see line 12214) and the reduced GL posting (see line 12253). |
| 12445 | `alTaxPortalDeleteAccountingRecord` | Generic hard-delete-by-table-name for sales/payroll/paychecks/contractor payments/manual JE, with GL cleanup. | MISSING | Explicitly not ported — `accounting.routes.ts` module doc comment: "Still not ported: alTaxPortalDeleteAccountingRecord (generic hard-delete-by-table name endpoint — risky in kind and in shape, skipped...)". Checked entire `accounting.routes.ts` — no generic delete route exists. |
| 12526 | `alTaxPortalSaveManualJE` | Creates a multi-line manual journal entry with debit=credit validation, posts GL lines. | FULL | `accounting.routes.ts` POST `/journal-entries` (lines 793-847) — same ≥2-line requirement, same balance check (±0.009 tolerance), same per-line insert + GL post. |
| 12602 | `alTaxPortalSaveCOAAccount` | Creates/edits a chart-of-accounts entry (admin only). | FULL | `accounting.routes.ts` POST `/coa` (lines 165-205). |
| 12647 | `alTaxPortalSaveTaxRate` | Creates/edits a tax rate, global or client-scoped (admin only). | FULL | `accounting.routes.ts` POST `/tax-rates` (lines 78-135) — same scope/global-vs-client resolution and upsert-by-existing-row logic. |
| 12725 | `alTaxPortalDeleteTaxRate` | Hard-deletes a tax rate row (admin only). | PARTIAL | `accounting.routes.ts` substitutes POST `/tax-rates/:rateId/deactivate` (lines 149-159), which sets `active=false` instead of removing the row. Functionally similar (rate no longer applies to new calculations via `lookupRate()`, which filters `active=true`), but GET `/tax-rates` (line 138-141) returns ALL rows with no active filter, so deactivated rates remain visible in the list where legacy's hard-deleted rows would be gone entirely. |
| 12759 | `alTaxPortalSaveTaskRule` | Creates/edits a task rule (admin only). | FULL | `src/modules/rules/rules.routes.ts` POST `/` (lines 102-140). |
| 12805 | `alTaxV5TaskDuplicateExists_` | Checks whether a non-terminal task already exists for a client/name/period. | FULL | Inlined in `rules.routes.ts` POST `/:ruleId/batch` (lines 181-187) — same status exclusion list. |
| 12818 | `alTaxV5ClientMatchesRule_` | Checks whether a client's field value matches a rule's trigger column/value. | FULL | Ported (and improved) as `clientMatchesRule()` in `rules.routes.ts` (lines 86-97) — module comment documents a deliberate bug-fix vs. legacy's silent "unrecognized column → treated as no-trigger → matches everyone" behavior. |
| 12826 | `alTaxV5ManualTaskRuleForClient_` | Finds a task rule matching a manually-created task's type AND the client's trigger conditions. | PARTIAL | `src/modules/tasks/tasks.routes.ts` POST `/` (lines 116-124) auto-applies a matching rule's defaults by `task_type` name only — it does NOT check whether the client's trigger-column value actually matches the rule (legacy's `alTaxV5ClientMatchesRule_` check), so a rule meant only for clients matching a specific trigger (e.g. a specific state or filing frequency) can incorrectly apply its defaults to any client's manually-created task of that type. |
| 12841 | `alTaxPortalCreateTaskBatch` | Creates a batch of tasks across matching clients from a rule, skips duplicates, notifies staff. | PARTIAL | `rules.routes.ts` POST `/:ruleId/batch` (lines 156-240) fully ports the matching/dedup/create/batch-log logic and adds a `dryRun` preview mode not present in legacy. However, the staff email notification side-effect (`alTaxV5NotifyStaffTaskBatch_`, called at line 12947 outside this chunk) is explicitly not ported — no outbound email infrastructure exists in the new app, so assigned staff are not notified when a batch creates their tasks. |
| 12954 | `alTaxV5ArchiveTaskRow_` | Copies a task row into Archived_Tasks and deletes it from the live table. | FULL | Ported as `archiveTask()` in `src/modules/tasks/tasks.routes.ts` (lines 220-236) — same insert-then-delete pattern, same field copy. |
| 12969 | `alTaxPortalRestoreArchivedTask` | Restores an archived task back into the live tasks table. | FULL | `tasks.routes.ts` POST `/:taskId/restore` (lines 299-321). |
| 13002 | `updateUnifiedTaskStatus` | Updates a task's status; sets FiledDate on completion; auto-archives on completion/closure. | FULL | `tasks.routes.ts` PATCH `/:taskId` (lines 245-285) — status is one of `TASK_UPDATABLE_FIELDS`, and completion triggers the same auto-archive call. |
| 13036 | `alTaxV5EnsureTable_` | Ensures a Google Sheet tab exists with the right header row/formatting. | N/A-SHEET-ONLY | Google-Sheets-specific bootstrapping mechanics; a Postgres app uses SQL migrations instead. |
| 13061 | `alTaxV5EnsureProfessionalTables_` | Ensures a fixed list of sheet tabs exist, seeds default COA. | N/A-SHEET-ONLY | Same as above — sheet/tab bootstrapping. |
| 13083 | `alTaxV5DeactivateUsersForClient_` | Deactivates every portal user assigned to a given client. | N/A-INTERNAL | Inlined directly as `UPDATE altax.v3_users SET active = false WHERE assigned_client_id = $1` in `src/modules/clients/clients.routes.ts` (line 257), inside the archive-client route. |
| 13091 | `alTaxPortalArchiveClient` | Archives a client: sets Status=Archived, disables portal, appends a note, deactivates its portal users. | FULL | `clients.routes.ts` POST `/:clientId/archive` (lines 244-265) — all four effects present, including the deactivate-users step. |
| 13112 | `alTaxPortalDeleteClientHard` | Permanently deletes a client row, gated by a "DELETE CLIENT" confirm-text field. | MISSING | Explicitly not ported — `clients.routes.ts` comment: "Legacy also has alTaxPortalDeleteClientHard... intentionally NOT ported here: it's an irreversible destructive operation... Archive is the safe, reversible equivalent." No hard-delete route exists in `clients.routes.ts`. |
| 13131 | `alTaxV5IsTestRecord_` | Heuristically flags rows as seed/local-preview test data by scanning several text fields. | MISSING | Checked across `src/` — no equivalent test-record-detection logic anywhere in the new app. |
| 13156 | `alTaxPortalClearTestData` | Deletes all rows flagged as test data across ~15 tables, gated by a confirm-text field. | MISSING | No "clear test data" endpoint exists — grepped `src/` for `clear.*test`, `test.*data`, `TestRecord` (case-insensitive): only unrelated match in `statementPdf.ts`. `src/modules/system/system.routes.ts` (the closest analog module) only has `/table-counts`, `/security`, and `/seed-defaults` — no data-clearing route. |
| 13192 | `alTaxV5MenuClearTestData` | Spreadsheet custom-menu UI wrapper (prompt dialogs) around `alTaxPortalClearTestData`. | N/A-SHEET-ONLY | Google Sheets `SpreadsheetApp.getUi()` menu/prompt mechanics; the underlying feature it wraps is itself MISSING (see line 13156) and there is no web-app menu equivalent to speak of. |
| 13214 | `alTaxV5FindUserRow_` | Finds a portal user's sheet row by UserID or email. | N/A-INTERNAL | Inlined as `queryOne` SQL lookups by `user_id` in `src/modules/users/users.routes.ts` (e.g. line 94). |
| 13231 | `alTaxV5FindUserRowByEmailRole_` | Finds a portal user's row by email+role combination. | N/A-INTERNAL | Inlined in `users.routes.ts` POST `/` (lines 93-95) as a combined email+role `queryOne`. |
| 13244 | `alTaxV5FindDuplicateUserForEmailRole_` | Finds another user row with the same email+role (to block duplicates). | FULL | Ported as the duplicate-check query in `users.routes.ts` POST `/` (lines 83-91) and again in `src/common/portalUserProvisioning.ts` (lines 40-44) for employee portal provisioning. |
| 13262 | `alTaxV5DuplicateUserMessage_` | Formats the "email already used for a X portal user" error message. | N/A-INTERNAL | Inlined as the literal error string in `users.routes.ts` (lines 88-90), same wording/structure. |
| 13267 | `alTaxPortalSaveStaffUser` | Creates/edits a staff/portal user; issues invite tokens; blocks duplicate email+role. | FULL | `users.routes.ts` POST `/` (lines 68-151) — same upsert-by-id-or-email+role logic, same 7-day invite-token issuance rule, same duplicate blocking. |
| 13343 | `alTaxPortalDeleteStaffUser` | Deactivates a staff user (despite the "Delete" name, it's a soft delete: Active=Inactive). | FULL | `users.routes.ts` POST `/:userId/deactivate` (lines 162-172) — same soft-delete behavior. |
| 13357 | `alTaxPortalHardDeleteUser` | Permanently deletes a portal user row (blocked only from deleting your own account). | MISSING | Explicitly not ported — `users.routes.ts` comment: "That is intentionally NOT ported here, for the same reason alTaxPortalDeleteClientHard wasn't: irreversible on live production data... Deactivate is the safe, reversible equivalent." No hard-delete route exists. |
| 13376 | `alTaxV5FindEmployeeRow_` | Finds an employee row by EmployeeID. | N/A-INTERNAL | Inlined as `queryOne ... WHERE employee_id = $1` lookups in `accounting.routes.ts` (e.g. lines 884, 940, 1023). |
| 13383 | `alTaxV5EnsureEmployeePortalUser_` | Creates/updates a portal-user account tied to an employee, issues invite token. | FULL | Ported as `provisionEmployeePortalUser()` in `src/common/portalUserProvisioning.ts` — same deterministic `emp_<employeeId>` user id, same duplicate-blocking, same 7-day invite-token convention; invoked from `accounting.routes.ts` POST `/employees` when `grantPortalAccess` is true (lines 917-924). |
| 13429 | `alTaxPortalSaveEmployee` | Creates/edits an employee OR contractor profile with full worker-type/tax/bank/1099 field set. | PARTIAL | `accounting.routes.ts` POST `/employees` (lines 870-930) exists and handles name/email/phone/payType/workerType/formType/status/wages/rate/hours/frequency/serviceCategory, but its write surface deliberately excludes SSN, EIN, TIN, Address, W9Status, TINVerificationStatus, VendorClassification, ContractorPaymentType, FixedProjectAmount, `1099Eligible`, and all PaymentMethod/DirectDeposit/bank-detail fields that legacy's version wrote. These columns exist in `v3_employees` (confirmed via `sql/schema_source.json`) and are READ elsewhere (e.g. 1099-NEC generation at line 1046, paycheck print at lines 641-642), but grepping the entire `frontend/src/` tree for `tin`, `ssn`, `w9`, `VendorClassification`, `FixedProjectAmount` returns zero matches — there is no write path anywhere in the new app (API or UI) to populate these fields for an employee/contractor. |
| 13516 | `alTaxPortalSaveContractorPayment` | Records a contractor payment with method/bank details, posts 2 GL lines. | PARTIAL | `accounting.routes.ts` POST `/contractor-payments` (lines 680-736) matches validation (contractor must belong to client and be flagged contractor/1099), amount>0 check, and the 2-line GL posting, but the INSERT (lines 708-721) never writes `ConfirmationNumber` even though that column exists in `v3_Contractor_Payments` (confirmed via schema) and legacy explicitly captures it. |

## Notes on N/A-INTERNAL and N/A-SHEET-ONLY items
These are not gaps — they are legacy-only implementation-detail helpers (sheet-row lookups, field-fallback readers, GL-line-copy helpers, Google-Sheets bootstrapping/menu mechanics) whose behavior is either inlined directly into the new app's SQL-based routes (confirmed by reading the call sites) or has no meaning outside a spreadsheet backend. They are listed for completeness only.
